# Phase 7: installable, documented and auditable release candidate

This ExecPlan is a living document. Maintain Progress, Discoveries, Decision Log and Outcomes. It implements Milestone 5 of `2026-09-05-mvp-completion.md`. It is prepared while Phase 6's frozen acceptance runs; it does not authorize Phase 7 source changes before that gate passes.

## Purpose and user-visible result

A fresh user can install a built distribution outside the source checkout, obtain the documented runner assets, initialize a disposable project with an explicit isolation boundary, talk to CoS, approve exact actions, inspect/apply evidence-backed code and review/apply/roll back organization proposals. The detailed guide distinguishes simulated evidence, real isolated command execution and live-provider behavior. Release evidence maps every in-scope MVP criterion to actual current tests and preserves the unperformed external prerequisites.

The end state is an implemented and verified local MVP/release candidate, with `docs/USER_GUIDE.md`, supported-platform and packaging evidence, reproducible contributor/CI checks, and the accepted change merged into GitHub. It is not a package publication or an invented license/live-provider approval.

## Scope

### In scope

- Close F7-01 through F7-14 and U1–U12/S01–S11 in `docs/MVP_ACCEPTANCE.md`, with evidence-specific limitations rather than aggregate-only claims.
- Complete detailed user and contributor guides, packaged runner recipe/access instructions, changelog, release/security/data-handling/dependency policies and a useful minimal architecture diagram.
- Fresh wheel and sdist installation into separate environments, with declared dependencies, outside the source checkout; installed executable/resource/schema/migration/offline-flow checks.
- Linux/macOS CI for the declared Python 3.12–3.14 range where feasible, actual Linux native-publication/E2E execution, local Docker recovery and public journeys. Document Windows/remote-filesystem limits.
- Bounded event/artifact scale smoke with measured timings/storage/memory and exact ordering/read-back; realistic schema upgrade records; security/adversarial release command.
- Repair the stale opt-in live-provider test's setup and enforce independent Docker prerequisites, but do not run it without a separately supplied credential and explicit opt-in.
- Final frozen quality/default/offline/integration/Docker/package checks, release ledger, commit/push/PR/merge and remote identity/post-merge verification.

### Out of scope

- Choosing a license, publishing a package or tagging a public release without separate owner authority. No LICENSE will be invented.
- Finding/reusing ambient API keys or contacting an actual model provider. Dependency installation and GitHub CI coordination are distinct from inference and remain ordinary authorized implementation steps.
- Modal/hosted providers, domain egress, new harnesses, external connectors, autonomous remote writes, daemon/scheduler, signed audit or arbitrary executable skill distribution.
- Mandatory telemetry/exporters. Optional local OpenTelemetry-compatible instrumentation will be explicitly deferred unless a measured current need justifies a bounded implementation.

## Starting repository state (historical planning snapshot)

The development branch is `codex/mvp-completion`, pushed through accepted Phase 5 checkpoint `a46b68800fb9725274db869f8c360b97a70c24ef` (tree `d99b632814b55d2a95b13e706b87d2017578c827`), while GitHub main remains `c700de1fc357844113426d3352cf29b6ffeae0f1`. Phase 6 code is implemented but acceptance/checkpoint is still running under `2026-09-05-versioned-fleet-evolution.md`; record its exact accepted commit/tree here before implementation.

The source package is `src/agent_fleet`, built with Hatchling and installed as `fleet`. `pyproject.toml` declares Python >=3.12,<3.15 and bounded runtime/dev dependencies; `uv.lock` pins the contributor environment. `tests/integration/test_distribution.py` builds wheel/sdist offline, compares every package source byte and README, validates schemas/migrations/prompts and imports wheel files against the existing locked dependencies. This is not a fresh dependency install. The current catalog has 82 schemas and eight migrations.

The only runner recipe is `tests/docker/Dockerfile.runner`, excluded from the sdist. It copies a `python:3.14-slim` base rootfs into `scratch` to strip inherited environment variables, then sets only the fixed allowed environment. The Docker adapter supplies the non-root user, command, no-network/read-only/resource limits and inspects effective state. The current base tag is mutable; the local cache identifies its image as `python@sha256:cad9a2c871761c413caa6fdd6441c783451e740a48aaeba60ae62a8b53525ef6`, but that local observation alone does not establish a portable multi-platform manifest. The current Python/pytest acceptance runner was prepared offline and is not yet a packaged fresh-user recipe.

`.github/workflows` is empty. `CONTRIBUTING.md` is minimal. No user guide, changelog, release process, measured scale test or standalone adversarial script exists yet. `tests/live/test_provider_smoke.py` still attempts public init with FakeSandbox, which cannot pass the isolated bootstrap gate; repair its test setup without relaxing public init. Existing live-network tests must continue to skip without explicit inputs.

## Security impact

Preserve all AGENTS.md security invariants and ADRs 0001–0006. This phase does not expand model tools, project permission ceilings or sandbox selection. Packaged resource access must not execute Docker, install dependencies, pull images or run project code implicitly. Any resource export must be an explicit user request into a new safe destination, reject links/existing conflicting files and report exact output identities; prefer a small read-only installed-resource interface if that satisfies the usable quickstart.

Dependency/image preparation can access package registries only as an explicit operator/CI setup step. Ordinary tests remain offline with real model requests denied. CI uses minimum read permissions for checks, no secrets or live inference, pinned/reviewed action identities and frozen dependency resolution. Untrusted pull requests must not gain write tokens or Docker host secrets. The final GitHub merge is separately user-authorized delivery, not an agent workflow capability.

Performance and adversarial fixtures use disposable Git repositories and state directories; never touch user projects, home/credential roots or global Docker resources. Cleanup targets exact owned IDs/labels/paths, not prune/glob operations. Provider data disclosure and local retention must reflect actual bounded context/artifacts/conversations/proposals, not claim all project data stays local when BYOK inference is enabled.

## Proposed design

1. Freeze the accepted Phase 6 checkpoint. Consolidate release requirements and guide structure before implementation; preserve its complete-tree, generation, cleanup and protected-registration semantics.
2. Ship minimal install-accessible runner resources under `src/agent_fleet` with a reviewed version/digest policy and explicit operator build instructions. Verify resource access from wheel and sdist installations, not source-relative `tests/` paths. Choose pinned base and dependency hashes based on current primary registry/lock evidence; never imply a local architecture-specific image proves all platforms.
3. Separate online dependency preparation from offline installation/execution. Build a wheelhouse from exact exported locked requirements in an explicit setup step. Fresh installation tests use separate new environments, no system-site-packages, `--offline`/`--no-index` and the prepared wheelhouse. Both wheel and sdist must resolve all declared dependencies, launch the installed `fleet` outside the checkout and show exact packaged assets. The normal suite may gate an unavailable prepared wheelhouse explicitly, but the required release selection must actually run, not silently skip.
4. Add a bounded scale test using real SQLite and content-addressed artifacts. Record append/list/page/read/status timing, ordering, content hashes, database bytes and peak memory for a fixed documented event/artifact count. Broad conservative time ceilings catch pathological behavior without treating one host's latency as an SLA. If a real unbounded/quadratic path is found, document and make the smallest relevant repair with tests before acceptance.
5. Add realistic upgrade/reopen cases covering paused approvals, leases, budgets, conversation/graph claims and organization versions. Upgrade only a test-owned prior-schema fixture; preserve wire bytes and events. Older binaries refuse schema8+. Recovery must retain prior non-replayable ownership.
6. Add a standalone adversarial verification entry point that runs named offline security regressions and, only with explicit Docker inputs, a real canary/sentinel/no-residue journey. Reuse existing tested fixtures/services; do not implement a second execution engine or make ordinary commands quietly install tools.
7. Write the complete guide and CI/release policies, then execute guide flows from fresh installed artifacts. Repair the optional live test to require an explicitly prepared Docker image and live model credential; fake-only assertions cannot stand in for verified completion. Exercise the setup offline with FunctionModel where practical; leave actual inference unperformed.
8. Freeze all implementation/tests/assets. Run all local gates and Linux/macOS CI where available. Review exact final diff and packaged bytes for secrets/machine-local paths, update evidence, then commit/push/create the authorized PR, wait for actual checks and merge. Read back remote main and run proportionate post-merge smoke. Never infer merge from a pushed branch or visible PR.

## Public contracts

- Keep all existing CLI and v1alpha1 JSON/error contracts; add only the smallest useful installed runner-resource access surface if needed, with real behavior and tests.
- No new model/provider, permission action, network capability, executable skill or sandbox backend.
- Package runtime/guide resources with documented installed access, exact manifest/version/hash where appropriate. Keep developer fixtures/acceptance secrets/local paths outside distributions.
- A new opt-in installation-release marker/environment may select prepared dependency artifacts; no automatic network fallback. Define its exact name/inputs in this plan before test implementation.
- Repair live-provider prerequisites in `tests/conftest.py`/the live canary without changing default skip safety. No production test-only bypass switches.
- Advance whole-phase metadata only after corresponding acceptance; package version/tag/publication policy remains a separate explicit release decision.

## Milestones

### Frozen minimal distribution/resource contract (before source implementation)

Use standard `importlib.resources` from an explicitly activated fresh Fleet virtual environment; do not add a new resource-export command, filesystem writer or model tool. Package one canonical `src/agent_fleet/assets/runner/` build context containing `Dockerfile`, `requirements.lock`, `.dockerignore` and its operator README. The guide obtains its installed path using `python -c "from importlib.resources import files; print(files('agent_fleet').joinpath('assets/runner'))"`. The operator independently chooses to build from that context. No automatic image pull/build or dependency installation is introduced into Fleet.

The default recipe pins `python:3.14-slim@sha256:cad9a2c871761c413caa6fdd6441c783451e740a48aaeba60ae62a8b53525ef6`. A fresh registry `docker manifest inspect` on 2026-09-05 established OCI index membership for Linux amd64 (`sha256:810da6270e43d30a1f3e0e1eabbeb6fbd9d78ad9dd2e754d5297a3d6cb42df46`) and arm64/v8 (`sha256:964225e67be639ec050dc6ce66ac0958b67e4ce0603e9847b2fae34cbf23f848`), among other platforms. Only observed test platforms will be claimed supported; manifest membership is not execution proof. The local Docker CLI has no usable buildx plugin, so the initial `buildx imagetools --raw` failed125; normal manifest inspection succeeded without pulling an image.

Install genuine runner-only Python test dependencies with exec-form pip invocation, explicit official index, isolated configuration, no cache/compile, exact versions and required wheel hashes: pytest9.1.1, iniconfig2.3.0, packaging26.3, pluggy1.6.0 and pygments2.21.0, taken from `uv.lock`. Then copy the resulting rootfs into scratch and set only the existing fixed PATH/LANG/LC_ALL. Keep the current source-only Dockerfile as an exact tested compatibility copy if needed; the packaged context is canonical. Record actual output image identity; reproducible reviewed inputs do not imply byte-identical layers across different builders/platforms or vulnerability-free images.

Package a small public learning fixture under `assets/canary/` with a deliberately broken text-only divide function, genuine pytest checks, pyproject test discovery and cache ignores. It contains no credential/sentinel/private developer state and has no executable setup hook. This explicit user example is distinct from the private bootstrap security canary. Users copy it only into a newly created destination, initialize Git and commit their own fixture before public Fleet init. Normal fake runtime can then demonstrate real-Docker approval/evidence/code apply without a model key. Do not register a test-only initialization bypass as a public workflow.

Keep `docs/USER_GUIDE.md` canonical. Build configuration may include this exact file in the wheel's `agent_fleet/assets/USER_GUIDE.md` and in the sdist without duplicating handwritten content; archive tests compare its bytes. Adjust the existing broad development-fixture archive exclusion only for the deliberately named public `assets/canary/` test files. Ordinary source tests, `.agent/` and machine state remain excluded. Source users read the repo guide; installed users can read the bundled copy through standard resources.

Use explicit marker `installed_distribution`, `AGENT_FLEET_ENABLE_INSTALL_TESTS=1` and `AGENT_FLEET_TEST_WHEELHOUSE=<absolute prepared directory>` for new fresh-install release cases. Default tests skip these cases; opt-in with incomplete inputs fails instead of silently skipping. Prepare wheels separately with frozen hash-bearing uv export and an exact pip tool (PyPI observed pip26.2.1, wheel SHA256 `71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e`). Each wheel/sdist case creates its own non-system-site-packages environment/cache and installs with offline/no-index/find-links and constrained dependencies; never fall back to the working `.venv`. A normal offline installed test can prove preview/refusal/migration/test-seeded fake flow, but must label that seed as test-only. Public installed bootstrap/approval/real code evidence requires a separately opted-in Docker case, using the packaged learning fixture and installed CLI.

Pin the reviewed build backend to Hatchling1.32.0 (PyPI observed wheel SHA256 `0e17c9c3b9aa7c625acc8d0f5b622f107d5049af9ecf5ada4de1aada5be7cdbc`) and include it in dev lock resolution so offline archive checks have their build toolchain prepared. Its declared build dependencies include packaging/pathspec/pluggy/tomlkit/trove-classifiers; record exact resolved versions/hashes with the wheelhouse and constrain the release build. No dependency/security claim is made just from reading PyPI metadata.

CI uses Ubuntu24.04 and macOS14 with Python3.12/3.13/3.14, explicit frozen setup followed by offline tests, no live key, checkout credentials disabled and contents:read only. Fresh GitHub release/tag read-back on 2026-09-05 resolved actions/checkout v7.0.1 to `3d3c42e5aac5ba805825da76410c181273ba90b1` and astral-sh/setup-uv v10.0.1 to `20cfd1bf945f4377ade1205e4dbc17946fc9a30d`; inspect their actual action inputs/runtime before authoring CI, pin exact hashes and uv0.12.9. A separate Linux Docker job builds the packaged recipe explicitly and runs native/Docker release cases. All resulting CI jobs remain unperformed until actual dispatch/read-back.

Primary preparation references: [Docker digest/build guidance](https://docs.docker.com/build/building/best-practices/#pin-base-image-versions), [uv environment locks](https://docs.astral.sh/uv/pip/compile/), [uv CLI](https://docs.astral.sh/uv/reference/cli/). Runtime and test behavior must be verified against the installed tool's actual supported flags, not guessed from a newer online reference.

### Milestone 1: usable packaged quickstart and detailed guide

Acceptance: installed users can find runner assets and follow documented setup, preview, verified bootstrap and explicit credential boundaries. The guide covers architecture/modules, six differentiators, minimal team choices, permissions, evidence, approval/restart, code review/apply, FleetPatch lifecycle and recovery. Examples match actual help/JSON/config models and distinguish historical unperformed gates.

### Milestone 2: installation, upgrade, scale and adversarial proof

Acceptance: both distributions install with declared locked dependencies into fresh isolated environments and pass representative offline CLI/resource/schema/migration flows outside the checkout. Scale measurements and realistic upgrade/recovery tests pass. Adversarial script has explicit offline/Docker modes and retains an evidence-backed result. Live test setup is valid but cannot run without both gated inputs.

### Milestone 3: platform CI and final frozen release-candidate matrix

Acceptance: Linux/macOS/Python matrix executes where feasible; Linux native whole-tree exchange and CLI recovery are observed rather than mocked. Windows limitations are explicit. Full frozen formatting/lint/types/unit/contracts/integration/offline E2E/default/Docker/package gates pass; state and Docker residue read-back is clean. Any unsupported platform or environment failure is retained as a precise limitation/blocker, not hidden by retries or broad skips.

### Milestone 4: final GitHub delivery

Acceptance: completed docs and evidence are committed, branch pushed with identity read-back, PR/checks reviewed, change merged and remote main verified. Final post-merge smoke records the resulting commit/tree. No public release/license/live-provider success is claimed unless separately authorized and proved.

## Detailed implementation steps

1. Record the final Phase 6 commit/tree and inspect clean/dirty ownership before choosing the next single-writer file boundary.
2. Finalize runner-resource contract, file names, base/dependency identity and explicit export/read mechanism here. Add packaged assets and narrow tests; update source-only Docker recipe to reference the same canonical content without drift.
3. Add installation release tests and dependency preparation instructions, with exact frozen lock/wheelhouse validation and no undeclared/system dependency fallback.
4. Add `tests/integration/test_state_scale.py`, realistic upgrade tests and the security verification entry point; capture normal outputs as structured metrics/evidence without secrets.
5. Repair the optional live-provider Docker setup and add offline verification of its prerequisite enforcement; do not invoke a real provider.
6. Write `docs/USER_GUIDE.md`, runner/data/dependency/security/release documentation and changelog; refresh CONTRIBUTING, README and the acceptance ledger. Use minimal helpful diagrams/tables and executable examples, not placeholder features.
7. Add minimal GitHub CI, then run local/platform/fresh-distribution/Docker gates. Record actual action/interpreter/Git/platform versions and failed attempts.
8. Freeze final content, complete review, perform authorized commit/push/merge and exact remote/post-merge read-back. Update this plan, parent plan and final delivery evidence.

## Validation plan

Keep AGENTS.md quality commands valid: `uv lock --check`, `uv sync --all-extras`, Ruff format/lint, strict mypy, generated-schema `--check`, unit/contract/integration/E2E/default pytest, `uv build --offline`, distribution tests and `git diff --check`. Execute named new installation/scale/upgrade/adversarial tests separately with exact recorded inputs. Real Docker selections require an explicit already-local image and shared test location; no live key is inferred. Freeze source/tests/assets and report overlaps honestly rather than add selection counts.

Fresh installed smoke must prove imported package/CLI paths belong to the new environment and cannot resolve checkout modules, all schemas/migrations/prompts/resources match archive bytes, ordinary network/provider requests are disabled, fake evidence stays unverified, and representative JSON errors have stable codes. Installed Docker guide proof is separate from dependency-free fake CLI proof. Check all managed-container identities, distinct acceptance databases' outstanding leases and pending organization fences after execution.

## Rollback and recovery

All test/install/build locations are newly created and exact scoped. Preserve failed environments/logs when useful; never delete a broad cache or user worktree to make installation pass. Source recovery uses commits, not resets. State migrations are forward-only; do not run older Fleet binaries against a newer database. Users must preserve the entire organization/state/receipt set before recovery; FleetPatch rollback is not database downgrade. GitHub delivery uses normal non-force branch/PR operations; do not overwrite remote user work or merge failing checks.

## Progress

- [x] (2026-09-05) Read release deliverables/acceptance protocols, current package configuration, runner recipe, distribution smoke, live-provider setup and contributor instructions while Phase 6 gates run. Prepared this living plan before Phase 7 implementation.
- [x] (2026-09-05) Drafted `docs/USER_GUIDE.md` in Chinese with retained English commands/contracts while Phase 6 remains source/test-frozen. Checked current CLI help, permission/chat handlers, configuration defaults, budget/context models and recovery flags. The guide explicitly labels pending installed-resource/platform/live/release evidence; no Phase 7 source implementation or fresh-user acceptance follows from this draft. Finish its runner/install/live sections after the corresponding implementation, then replay documented flows.
- [x] (2026-09-05 17:08 UTC) Prepared a separate network-enabled dependency wheelhouse from the unchanged current hash-bearing uv export, with pip26.2.1/require-hashes/only-binary and official index. Download completed48 applicable packages (Windows/Emscripten markers excluded). Initial uvx default selected Python3.13, so wheels alone do not establish this workspace's Python3.14 fresh-install prerequisite. Repeated preparation explicitly selects the observed3.14 interpreter; the final script must bind and report its target interpreter/tags. This is preparation, not fresh-install execution, and build-backend additions remain pending the Phase 7 lock update. No actual provider key/request was used.
- [x] (2026-09-05 17:08 UTC) Fresh GitHub read-back confirms development branch still a46b688 and main c700de1, no existing PR for this branch, repository visibility PRIVATE and Actions/CI not yet run. Preserve visibility; user authorization to push/merge is not authorization to make the repository public. Choose standard hosted jobs with bounded time/concurrency, not paid larger runners.
- [x] (2026-09-05 17:25 UTC) Phase6 accepted and committed/pushed as `b42cdf9ccf5325acc99bf7fb43581329045c0490`, tree `bb5e669f9ce77ae45778b56c2286b4661b353a97`; remote branch exactly matches, main remains c700de1, checkout clean. Final metadata/archive36/30.70s and README/archive6/2.31s passed after1973 default and14 Docker passes. Begin Phase7 under the frozen resource/install contract above, with root as sole writer. No provider calls, license choice, visibility change or new runtime authority.
- [x] (2026-09-05 18:04 UTC) Implemented the candidate's packaged runner/learning fixture and canonical bundled 16-chapter Chinese user guide, fresh wheel/sdist/public-Docker tests, schema7-to8 compatibility fixture, measured scale test, standalone security replay, explicit dependency preparation, six-platform CI matrix and release/data/dependency/security/contributor/changelog documentation. These implementations are not final acceptance. CLI phase metadata stays6; CI and final GitHub merge remain unperformed.
- [x] (2026-09-05 18:04 UTC) Built the packaged runner on local macOS arm64/Colima: `agent-fleet-runner:0.1.0-py314-v1`, image `sha256:2afebd51ae66f07096063b53fc14b6a45b18dd63b01d7f31ed0cb640c51da241`. Rebuilding after the build-stage pip configuration guard produced the same final image ID. This is local build evidence, not Linux-hosted CI or cross-builder reproducibility. Prepared the current lock55 wheelhouse with52 applicable wheels for Python3.14.6 at `/Users/blackswan/.cache/agent-fleet-phase7-locked-dependencies.zACwmg/prepared`; earlier51-wheel/lock54 and48-wheel/lock51 preparations are superseded, not fresh-install passes.
- [x] (2026-09-05 18:04 UTC) Focused assets/tooling/readiness/upgrade selection passed18 tests in12.76s; scale/readiness selection passed9 with one explicit live skip in3.36s. Actual scale observation:1000 appended events plus256 artifact events,256 artifacts totaling1MiB; append1.609682s, artifact writes0.536526s, page1256 events0.064872s, reads0.210839s, status0.004753s, DB+WAL5,844,544bytes, traced peak972,057bytes. These are one-host smoke measurements, not an SLA. Initial scale fixture failed1 with2 passes in15.32s because it treated artifact-created events as indexed scale events; filtering by the actual event type and asserting the full1256 sequence fixed the test without runtime changes.
- [x] (2026-09-05 18:04 UTC) Independent bounded release audit found and reviewed fixes for ambient Git filters in the installed fixture, global/site pip configuration despite isolated mode, and Linux's five Darwin-only security skips plus contradictory script exit reporting. The auditor returned PASS with18 passes/1 deselected in0.66s, explicitly excluding full installation/Docker/security/CI acceptance. Added a genuine pinned-pip configuration regression and `PIP_CONFIG_FILE=/dev/null` for dependency preparation and runner build only. No product execution boundary was weakened.
- [x] (2026-09-05 18:04 UTC) Retain fresh-install iterations: the initial wheel/sdist/public-Docker cases passed3 in53.59s, but this predates the audit's fixture hardening. The hardened replay failed3 in11.06s: its commit argv still injected forbidden `-c` overrides. Corrected the fixture to rely on the adapter's trusted identity/configuration and use explicit no-sign/no-hook flags; the production guard remains unchanged. A fresh replay is required. Latest completed focused tooling/assets/archive checks passed11 in2.31s; static checks passed267 formatted files,219 mypy files,82 schemas, lock55/sync53. The pre-repair source/test/script checksum `7b2b4b97a84c95853999476ff031095067868cca3973ebe1eefe3ba152f9e52e` is historical, not the final freeze.
- [x] Packaged runner access and detailed user guide accepted at the independently verified local installed/offline-model boundary; final platform/public-release claims remain separate.
- [x] (2026-09-05) Correctly gated hardened fresh-install replay passed3 in60.92s. A prior invocation used the wrong Docker image variable and returned2 passes/1 setup error in35.24s; no Docker command ran in that failed setup. Standalone offline adversarial replay passed539 in175.37s, zero skips/errors/failures, structured verdict0; source-manifest hash `20296b9283fb1f0a92bbf0ef7b463778aff1b05ca6bf5fcbacea8fb076081c78`. This precedes the additional installed-user coverage below and is not the final freeze.
- [x] (2026-09-05) Independent guide delta review accepted the Git fixture repair but found M1's installed journey narrower than its specified doctor/chat/retry/inspection/trust/evolution/recovery surface, and an undated README Phase6 status was stale. Before final freeze, extend only installed tests with those representative entry points, preserve the guide's Safe/src flags, and reuse an offline FunctionModel response fixture for organization proposals through the installed CLI. No new product fake capability, raw proposal injection, private init shortcut, real provider request or duplicate security engine. Installed model-substitution evidence must remain distinct from the stock no-key learning example.
- [x] Fresh wheel/sdist, realistic upgrade, measured scale and explicit offline/Docker adversarial proof passed at the recorded local identities; final full/platform acceptance remains required.
- [ ] Supported-platform CI and final frozen local/Docker matrix.
- [x] (2026-09-05 18:26 UTC) Expanded installed guide gate passed3 in92.32s: doctor, Safe/src public Docker init, chat/approval/retry/inspection, exact persistent-rule revoke, code apply and no-op confirmed recovery; separate public PydanticAI registration with only offline FunctionModel substitution proves installed proposal/diff/apply/inverse rollback/historical chat. Retained preceding1 failed/2 passed in80.80s: test substitution omitted BootstrapService's runtime registry; real preflight correctly returned CREDENTIAL_MISSING. Fixed only the test's model registry wiring. No actual credential or model request was used. CLI argv/exit/stdout/stderr evidence is retained per fresh installation, without environment values.
- [x] (2026-09-05 18:26 UTC) Real-Docker standalone replay passed14 tests/4 deselected in133.52s with structured verdict0 and zero errors/failures/skips. This selection does not include installed tests; its end manifest `6ca2926261e17d60d522b98580e5b0823e8e0e592a24909e6f8a38d7ce70a6e1` reflects concurrent installed-fixture edits outside the executed selection, so it is not labeled a full frozen-tree gate. Final source/test/script freeze is now `9eaa3dceefcab912434229a1783778a3f9ec9f7335d1b4d936d3e1e2f30449a4`; no further implementation changes planned before full gates. README current status corrected after the installed pass; refresh archive/fresh installation against those final documentation bytes.
- [ ] Independent final review and authorized GitHub merge/read-back.
- [x] (2026-09-05 18:29 UTC) Committed/pushed the release candidate as `371452a3ab6c805396bc6f1afffefad42cc11338`, tree `627c971e340bc1a4479e0522591af04425a4c6ac`; exact remote branch matches, main c700de1/private unchanged. GitHub push workflow33984182125 started actual nine-job verification; no PR/merge yet. README-frozen archive/schema/tooling/assets passed16 in2.63s and offline wheel/sdist build passed before commit. Source/test/script aggregate remains9eaa3dce.
- [x] (2026-09-05) Independent fresh installed replay passed3 in102.12s at unchanged9eaa3dce pre/post aggregate, JUnit `/Users/blackswan/.cache/agent-fleet-phase7-independent-install.DawdQm/results.xml`; reviewer is reading exact retained command/state evidence before final verdict. Frozen offline security539/212.06s and Docker14 with4 deselected/158.01s both passed with identical structured source-manifest hash `86e2a3b5048995b33291b014f22a6f28cd3af56c4872799c8d73dbfd5ea2d4f5`, zero skips/errors/failures, verdict0. Local complete default remains in flight against its originally collected candidate; it is not yet a pass.
- [x] (2026-09-05) Actual CI found one Linux typing issue in the preexisting Darwin-only test: `os.chflags` is absent from Linux type stubs even though pytest skips that fixture there. All three Linux static jobs failed that exact assertion; installed and Docker jobs passed, other jobs were still running. Preserve this first-run failure. Use a checked dynamic lookup of the same native setter in the Darwin-only fixture, without changing production code, acceptance assertions or platform skips. Repeat the whole native contract file and both Darwin/Linux mypy, then new-commit CI. The in-flight local default remains evidence for371452a, not the later typing-only test revision; final Linux complete-default CI must execute the updated tree.
- [x] (2026-09-05 18:34 UTC) Portable native-fixture delta passed75 tests in1.51s; Ruff267, strict mypy219 on both actual Darwin and explicit `--platform linux`, schema82 and whitespace checks passed. Updated aggregate `c3e12368e0800311af9b1618eeca0b4c7fd58397e1c35b8c5ae6e5849426f713`. The original CI's completed Linux Docker job101354593644 passed15 tests/6 deselected in152.46s (14 real boundary cases plus installed public journey), using Python3.14.7, Git2.55.0 and built amd64 image `sha256:ab336b5ace28048d6bd26bd762cf73306746592c2d49a128dda11bd906ac5733`. New branch CI will supersede/cancel incomplete old jobs under the declared concurrency policy; completed failures/passes remain historical evidence, not a full matrix pass.
- [x] (2026-09-05) Independent M1 read-back PASS: all85 Docker-state artifact blobs match persisted size/hash; two independent real five-test command receipts and exact code apply, native organization revisions0→1→2 restore original tree, six databases have zero outstanding leases/pending operations/active chat claims,42 historical leases released and exact managed Docker scope empty. JUnit SHA256 `25d3ea1faaecd12f79642ba51b1b75f2a9f87eff8b16ebc9a1a11f58585a737f`; command log SHA256 `5f722564ae06eef8232ed1aed285a58f22783854bb0094ec74704a02f1317a63`. Each fresh environment has36 locked distributions and82 schemas;218 normalized package/guide files match. Scope: genuine installed/native/Docker behavior with offline Fake/FunctionModel responses; confirmed recovery was a no-op, not an installed crash test. Source fresh-process crash and real Docker interruption recovery remain separate proving selections.
- [x] (2026-09-05 18:41 UTC) Portable test correction committed/pushed as `de0e2303039c0cc725c8b53418f4227e0321fb9f`, tree `d7ff017720d282823131e5de32ba6c603b9642a6`; exact remote branch matches. New workflow33984668495 has all six platform static gates passing, completed installed/Docker jobs and remaining full/E2E jobs still running. Workflow33984182125 is cancelled under concurrency, retaining its three static failures and completed installed/Docker passes; never treat cancellation as full acceptance.
- [x] (2026-09-05 18:43 UTC) Frozen Docker gate's read-only26-database audit found97 released/nine historical recovered leases, zero outstanding leases/pending organization heads or operations/active chat claims; globally no managed containers remained. The final guide has16 chapters/649 lines, nine local file links resolve and code fences balance. README/guide/pyproject/lock bytes remain exactly those of371452a and the independent fresh-installed pass; no package source or assets changed in the portability correction.
- [x] (2026-09-05 18:49 UTC) Second CI exposed further Linux failures after static checks passed: Python3.13 returned3 failed/1645 passed/5 expected Darwin skips in452.65s; Python3.12 returned the same in458.66s. Two unchanged Docker contracts unlink/recreate the static empty `.git` shadow, but `(device,inode)` alone may be reused on Linux and fail to observe replacement before container create or after the final daemon round trip. A tracked-organization fixture also used raw Git commit with no guaranteed author identity, hidden by local global configuration. Mac3.14's selected job passed; other jobs remained running. These are not accepted matrix results.
- [ ] Additional bounded Phase7 hardening slice, planned before production edits: bind only the immutable Git-shadow file to `(device,inode,mtime_ns,ctime_ns)` in the ephemeral prepared sandbox and repeat it at both existing dispatch checks. Retain a validated non-inheritable read-only file object for that prepared lifetime: timestamps may be coarse, whereas pinning the inode prevents its reuse after unlink. Close explicitly on failed preparation and successful logical destruction; preserve the pin while cleanup remains ambiguous, with standard file-object finalization as the dropped-provider fallback. Keep workspace identity at device/inode because legitimate candidate writes change directory timestamps. Preserve empty400/owned/regular/nlink1 checks and all existing unlink/recreate negatives; add deterministic same-inode timestamp-change cases at both cutpoints, legitimate candidate-write/repeated-execution positives and descriptor lifecycle tests. This is not an atomic Docker pathname handoff or protection against a hostile same-user/kernel. No new grants, public schema, persisted identity, provider calls or sandbox fallback. Fix the raw Git fixture through the existing hardened adapter's trusted identity, without caller configuration overrides. Obtain an independent delta review, then refreeze and rerun local full/default/security/native/installed/Docker plus CI; pre-repair passes remain historical because this slice changes production code.
- [x] (2026-09-05 18:59 UTC) Initial new local red-test attempt failed twice because its fixture omitted `import os`, not because it reached the security assertion; corrected that test-only import before obtaining the actual red proof. Independent pre-fix probes already reproduced both same-inode dispatches. Complete historical371452a local default finished1993 passed/18 explicitly gated skips in1512.74s; second de0e230 CI completed with five successful jobs and four failed Linux jobs, so no full matrix acceptance.
- [x] (2026-09-05 19:06 UTC) Corrected local red proof:2 failed/184 deselected in0.21s, both DID NOT RAISE at unchanged security assertions. Implemented descriptor pin plus metadata checks; original/new Docker contracts first passed186/0.41s. Added lifecycle positives/negatives initially found two test mistakes (hardcoded mock container name and missing FleetError remediation),2 failed/198 passed in18.79s, then corrected the mock to return its actual dispatched name and supplied the required fixture field. Final focused Docker contracts plus tracked/untracked proposal integration passed202/18.87s; Ruff267, mypy219 on Darwin and Linux, schema82 and whitespace passed. Independent delta review is running. Add the Docker contract module to the named offline adversarial script and execute that script in the Linux full-default CI job so these new regressions and exact Darwin-only skip rules are exercised by the public verification entry point too; keep its run separate from the overlapping full-default count.
- [x] (2026-09-05) Historical de0e230 workflow33984668495 final evidence: all three macOS platform selections passed1653 each (Python3.12.14/625.80s,3.13.15/558.59s,3.14.7/508.84s). Linux3.14 selection failed3/1645 passed/5 Darwin skips in558.08s; complete Linux3.14 default failed the same three cases,1983 passed/25 skips in973.32s. Five successful jobs include installed2/1 deselected68.81s and real Docker plus installed journey15/6 deselected180.74s. Git2.55.0 throughout. These precede the repair and remain historical, not acceptance for the new source.
- [x] (2026-09-05) Repaired source/test/script freeze `c03fa3d455b073958ade3c4d365a0231319e1b59eb715af3b79bb278bff5d3c3` (sorted repository-relative `rg --files src tests scripts` byte hashes). Static267/mypy219 on both platforms/schema82/lock55/sync53/whitespace passed. New full-default JUnit is running under `/Users/blackswan/.cache/agent-fleet-phase7-shadow-default.bHOzWY/default.xml`; expanded standalone offline evidence under `/Users/blackswan/.cache/agent-fleet-phase7-shadow-security.JsZkmO/offline`; real Docker evidence under `/Users/blackswan/.cache/agent-fleet-phase7-shadow-docker.7Unwca/audit`. These are in-flight, not passes. Checkpoint/push this bounded repair to obtain actual Linux CI; final PR stays draft/unmerged until all candidate gates and independent review pass.
- [x] (2026-09-05) Repair checkpoint committed/pushed and remotely matched as `329324430e4b712062f045fb613173cf4a8adea7`, tree `a6ac78acb11658d7dae8cddb10616f6ff140a5ed`. Created draft PR #5, `https://github.com/meyowu/agent-fleet-codex-kit/pull/5`, against main; private visibility rechecked. Cancelled only this thread's redundant push workflow33986200211 after the exact-head pull-request workflow33986257215 was queued. Cancellation is not a pass. All six PR static jobs and the installed/Docker jobs have passed so far; final conclusions remain outstanding.
- [x] (2026-09-05) Independent shadow delta PASS:277 focused Docker/native/proposal tests in20.01s plus no-dispatch probes at both mutation cutpoints, unlink/recreate with restored original mtime, actual read-only/non-inheritable descriptor checks, cancellation during termination retaining the pin, successful retry closing it, finalization and legitimate repeated workspace writes. Docker source hash remained `c1edff26d3440e3cced2a9a03c977dca2331483d9b134b4750c685e33340d4d5`. No daemon/provider/Linux/full-suite claim from this reviewer.
- [x] (2026-09-05) Repaired frozen standalone gates passed: expanded offline734 tests in199.76s (script201.222s) and real Docker14/4 deselected in148.37s (script149.690s); no skips/failures/errors and structured/process verdict0. Both script manifests are `8387db7309d7981c8e6b53ce52a46ad7459836d50534e7df6ea8f610e774f187`. Docker residue audit:26 distinct databases,97 released/nine recovered leases, zero outstanding/pending organization heads or operations/active chat claims; zero managed containers before the separate installed replay.
- [x] (2026-09-05) Repaired-source fresh installed wheel/sdist/public-Docker replay passed3 tests in105.72s under `/Users/blackswan/.cache/agent-fleet-phase7-shadow-installed.UPFdqX`; no live request. Archive/tooling/assets focus passed11/2.61s and offline wheel/sdist build succeeded. These precede the final README/guide consolidation; the production/test/script aggregate remains unchanged, but final documented package bytes must be refreshed after editing.
- [x] (2026-09-05) Documentation consolidation removes duplicated stale Phase1.5–5 snapshots from README, retaining links to their full living acceptance logs; current results now have one named table. Guide/roadmap explicitly retain phase6 until the unperformed public-license/live-provider gates, independently of authorized local candidate/GitHub delivery. A fresh bounded read-only guide/CLI/resource review is in progress. Do not infer a full public release from local or CI passes.
- [x] (2026-09-05 19:20 UTC) All six actual platform jobs at3293244 passed. Linux3.12.14/3.13.15/3.14.7 each passed1659 with five exact Darwin metadata skips in465.07/440.14/489.22s; macOS3.12.14/3.13.15/3.14.7 each passed1664 with no skips in446.19/505.08/515.57s. Git2.55.0 throughout. Linux Docker+installed public journey passed15/6 deselected153.57s using amd64 image `sha256:a5335772f0126e59e2cf5abe1fa0f44a21274697469a6309c4b76b97fa0309a3`; macOS fresh wheel/sdist passed2/1 deselected50.95s. The full Linux default and standalone script job still runs; do not count eight successful jobs as a nine-job verdict.
- [x] (2026-09-05) Fresh guide audit observed28 actual CLI help successes without designated state creation,47 resolved local links/anchors and balanced fences; two asset tests passed0.51s and archive/resource smoke passed1/2.03s. Fixed its README findings: unsafe fixed Colima basetemp replaced with fresh mktemp/child fixtures, migrations corrected to8, all five structured outputs named, pure no-I/O CoS hash helper distinguished from Gateway resource tools, and bounded organization Git reader distinguished from uncapped ordinary paths. Added read-only installed-guide discovery. Package bytes will be refreshed after final prose; these bounded checks are not fresh installation or full release acceptance.
- [ ] After the in-flight local behavioral suite finishes, correct only the pre-existing `fleet resume` help docstring that still says fake workflow. Keep implementation and phase marker unchanged; replay actual CLI help/CLI integration and archive/fresh-install gates. Bind the one-literal cosmetic delta separately, and require the final PR's complete Linux/default plus macOS/platform CI on the new exact tree before merge.
- [ ] Final delivery configuration: replace the temporary `push: codex/mvp-completion` trigger with `push: main`, retaining pull-request/main and manual dispatch triggers and every gate/permission/pin. Add the exact default-branch assertion to the existing CI shape test. The temporary branch trigger bootstrapped pre-PR CI but is not the maintained repository boundary; this change avoids duplicate branch/PR runs and enables actual post-merge main verification. Do it after the running local freeze completes, then run the final exact-head PR matrix and post-merge main workflow. Do not dismiss a cancelled workflow as passing or weaken checks to merge.
- [x] (2026-09-05) Repaired installed evidence read-back verified all171 artifact blobs across the mixed fake/Docker package cases by persisted size/hash, six databases with42 released leases and no outstanding/pending/active claims, and no managed containers. A native system sqlite3 read-only probe returned open error14; the project's locked Python SQLite read-only URI opened the same files and completed the structured audit without database mutation. No installed crash claim follows from the confirmed no-op recovery case.
- [x] (2026-09-05 19:29 UTC) All nine PR workflow33986257215 jobs completed successfully on3293244. Complete Linux3.14 default:1997 passed/25 skips in994.03s. Its skips are14 explicit Docker,three installed,one actual live-provider,five Darwin metadata andtwo case-insensitive-filesystem-only regressions. Standalone Linux script:729 passed/five exact Darwin skips in146.02s, structured734 selected, script148.672s, zero unexpected skips/errors/failures, process/verdict0 and the same `8387db...` manifest. This is actual full Linux acceptance and six selected platform combinations, not every integration on every combination. Local complete-default remains in flight; final doc/help/CI-trigger refresh and exact final-head checks follow before merge.
- [x] (2026-09-05 19:33 UTC) Full local repaired freeze passed2004 tests/18 explicit skips in1515.89s. JUnit SHA256 `ce5d7ef4fc7a9f10544a7b21f55418c4b6b297a69e934c2678a5a5dc0ce72c55`; partitions:unit1027,contract578,integration372,offline E2E22,four offline Docker-directory cases andone live-readiness case. Skips are14 Docker,three installed andone actual live-provider. They are not passes; Docker/installed opt-ins passed separately. Source/test/script c03fa3d remained unchanged; README/guide editing requires the planned separate documentation-final archive proof.
- [x] (2026-09-05) After that full suite exited, applied the planned final-only cosmetic/CI delta: one resume help docstring and its no-state help assertion in the existing CLI test; temporary branch-push trigger becomes main-push, with exact main/PR assertions in the existing CI test. No runtime behavior, grants, migration, dependency, phase marker or runner asset changed. Require CLI/static/archive/fresh-install refresh and the final exact-head PR matrix;3293244 remains the identified historical complete local run, not the new content hash.
- [x] (2026-09-05 19:38 UTC) Final source/test/script freeze `c400cdb40fa16bb13d2c1a8732893560ccc91db5fdbf916bdcf6868857378cf6`:41 CLI/tooling/archive/assets tests passed31.83s; Ruff267/mypy219 on Darwin and Linux/schema82/lock55/sync53/whitespace passed. Fresh wheel/sdist/public-Docker tests passed3/94.60s under `/Users/blackswan/.cache/agent-fleet-phase7-final-installed.EzciAj`. The current guide has16 chapters/655 lines. README now reports exact passes with all historical detail linked to plans; a final evidence-text-only package refresh follows, without further guide/runtime edits.
- [x] (2026-09-05) Independent final delta PASS: production AST is identical after removing only the changed resume docstring; actual help creates neither designated state nor containers. Workflow changes only the feature-branch push trigger to main; all nine gates/pins/read-only permissions/live-off settings remain. CI-shape test1/0.07s, installed-guide discovery and prior README corrections verified; c400cdb unchanged before/after. No new full/daemon/installed/remote acceptance is attributed to that bounded review.

## Discoveries

- Observation: current archive smoke imports wheel files using existing dependencies; prior clean-offline attempts lacked cached PyYAML. Consequence: prepare exact dependencies explicitly, then prove a new environment rather than relabel archive/import checks as fresh installation.
- Observation: README runner recipe lives only in excluded `tests/`, with a mutable Python base. Consequence: ship one canonical installed resource and reviewed digest/version strategy; distinguish base preparation from Fleet runtime behavior.
- Observation: the live canary still expects FakeSandbox public init and simulated completion. Consequence: repair setup/prerequisite enforcement, retain opt-in and never interpret offline responses as a live-provider pass.
- Observation: organization publication depends on POSIX/native directory exchange and supported metadata. Consequence: require actual Linux/native proof and document unsupported Windows/remote filesystems; do not weaken controls for a CI pass.
- Observation: pip's `--isolated` still reads global/site configuration, and raw fixture Git calls can load global attributes/filters. Consequence: the explicit preparation/build stage sets `PIP_CONFIG_FILE` to the null device, with an actual pip configuration regression; installed fixture creation uses Fleet's hardened Git adapter and an inert test-owned poison/sentinel configuration. Do not alter normal user Git configuration or relax adapter argv validation.
- Observation: five real Darwin metadata tests intentionally skip on Linux. Consequence: the security runner records only those exact module/name/platform skips as expected, retains them as unexecuted platform coverage and fails any other skip. Its structured verdict and process exit must agree.
- Observation: a pytest platform skip does not make an unsupported `os` attribute portable to mypy's Linux stubs. Evidence: all three first-run Linux static jobs rejected the direct `os.chflags` fixture reference. Consequence: explicitly check the optional native setter, retain the real Darwin operation and rejection assertion, and run type checking for both Darwin/Linux before repeating CI.
- Observation: Linux may reuse an unlinked regular file's device/inode while its static-shadow replacement has changed timestamps; raw Git fixture commits also depend on ambient author configuration. Evidence: two real path-replacement contract failures and one tracked-organization commit failure in both Linux3.12/3.13 jobs. Consequence: strengthen the static shadow fingerprint rather than weakening rejection tests or adding a platform skip; use the hardened Git adapter for fixture commits and repeat all relevant acceptance.

## Decision Log

- Decision: finish the authorized local MVP/release candidate and GitHub delivery while keeping owner-license and real-provider gates explicit. Rationale: neither authority was supplied, and the user requested no branding/license/provider-choice questions. Alternatives: invent license, reuse ambient keys or stop all independent work; rejected. Date: 2026-09-05.
- Decision: separate dependency/image preparation from offline execution and fresh-install acceptance. Rationale: ordinary tests must be offline; a cold dependency cache is an environment prerequisite, not proof of a bad package or permission to fetch during tests. Date: 2026-09-05.
- Decision: defer optional OpenTelemetry export/instrumentation unless measurements show a concrete need. Rationale: local structured events and retained evidence already serve this MVP, and mandatory external services are out of scope. Date: 2026-09-05.
- Decision: retain the already-running local full371452a gate while repairing only the Darwin fixture's static attribute lookup; require an exact-delta native contract replay and actual full/default CI on the new tree. Rationale: no production/build/resource behavior changes, the same native flag operation and negative assertion remain; report each identity honestly instead of discarding a useful full-platform run or pretending its manifest remained unchanged. Date:2026-09-05.

## Outcomes

Phase6 is accepted, committed and pushed as b42cdf9. Phase7's packaged quickstart/16-chapter guide and installation/upgrade/scale/adversarial milestones have local and independent evidence. Historical371452a full local default passed, but de0e230 Linux CI exposed a shadow-file inode-reuse gap and an ambient-identity fixture dependency. The bounded lifetime-pin repair has focused/static passes; renewed full/platform/security/installed/Docker acceptance and independent delta review are required before merge. Whole-phase metadata remains6. No actual provider request/key, public release, license selection or repository visibility change occurred. The real-provider and owner-license public-release gates remain explicitly unperformed.
