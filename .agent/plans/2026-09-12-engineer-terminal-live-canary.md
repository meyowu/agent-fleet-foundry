# One post-H Engineer terminal live canary

This living ExecPlan was created before dispatch, after H acceptance and Git
delivery. Maintain Progress, Discoveries, Decision Log and Outcomes. It permits
one bounded new observation, not an automatic continuation of failed live05.

## Purpose and user-visible result

Measure whether the already delivered Engineer strict-terminal contract supports
the original public CoS -> Engineer -> independent Verifier code-change journey
with the unchanged explicitly selected OpenAI models and three Harnesses.
Success requires a reviewable one-file patch, real independent Docker evidence,
complete accounting and proven cleanup; a model's completion message is not proof.
The target remains unapplied in this canary. Example entry point is the existing
`scripts/run_live_canary.py --run --selection <reviewed JSON> --image
agent-fleet-runner:0.1.0-py314-v1 --output <new private directory>`.

## Scope

### In scope

- One manually chosen whole-test attempt from clean accepted PR16 source,
  using the original test, launcher, fixture, selection and finite limits.
- A new secret-input wrapper differing from05 only in its two fresh output-name
  literals; actual accepted HEAD is supplied through its existing argument.
- Preserve all safe original evidence, including failures, with independent
  stopped-owner, typed state/artifact/accounting and exact cleanup readback.
- Update README, acceptance ledger and this plan with actual results, then
  commit/push/normal exact-head merge the bounded documentation slice.

### Out of scope

No model/selection/schema/prompt/permission/budget/dependency/product-code change,
automatic whole-test retry, hidden fallback, payload capture, original-target
patch application, package publication, provider expansion or Native P0 work.
No campaign or multi-task qualification is inferred from this single observation.

## Current repository state

H is delivered in PR16: head2324e4690fa41da0bd2aa136853a8c13383c8e95,
mergea4747deba988f9b8caa2169509fd0e774e7656ac, common tree
9d22fb00bb268d78c9c5395a6ddc48e7ff66c8c0. Remote main, local launching ROOT and
delivery worktree agree. All556 accepted source/README/guide inputs match and
postmerge129 original SDK contracts passed/3.06s. Full4296/32, six static gates,
Docker22+cohort6, adversarial806, package1 and installed3 plus independent reviews
are recorded in `2026-09-12-engineer-terminal-contract.md`; counts overlap.
Head and merge Actions counts are0, not remote CI success.

The original live05 remains NOT_PASSED with5 reported requests/17487 tokens,
three Engineer tools and one standalone passing Docker command, but no accepted
Engineer report, patch or Verifier. Its exact invalid payload is not retained;
H must not be called a proven diagnosis or retrospective repair of that failure.
Its stopped audit/cleanup does not change its original inconclusive bundle.

The launching checkout stays clean at accepted PR16. This plan is maintained in
a separate docs-only worktree so registration precedes dispatch without changing
the executed source. The generated disposable fixture is not the project itself.

## Security impact

The owner explicitly renewed use of the earlier supplied OpenAI key for project
testing. Root may supply it once through no-echo input to a trusted transient
test process. No credential enters files, command argv, subagents, model context,
Docker, Git or logs; inherited alternate endpoints and unrelated credentials are
not forwarded. No other-provider credential lookup or secret hunting is allowed.

Existing ToolGateway/PermissionBroker, strict output/semantic validation,
single-send accounting, role binding, sandbox isolation and approval rules remain
unchanged. The original disposable test approves only each observed exact request
with `--run`, up to12 approval iterations. This is not global user trust.

## Proposed design and public contracts

No public contract changes. Keep raw reviewed selection SHA256
8398f5f361d437abdb34c6891eb91f79a5c71d42b4bafe8d9a5729ea77fa205f and canonical
SHA2569de1b68b151c58a3752b9aa6a9705b05903be8933d59c500618c2bef6248d4bc.

| Role | Harness | Model |
| --- | --- | --- |
| CoS | PydanticAI | openai:gpt-5-nano |
| Engineer | OpenAI Agents SDK | openai:gpt-4.1-nano-2025-04-14 |
| Verifier | LangGraph | openai:gpt-4.1-nano-2025-04-14 |

All selected roles use the same existing opaque OpenAI credential reference.
Selection revision4 closes five configured bindings; only these three roles
execute. No alias or model substitution is allowed if availability changes.

One whole attempt, root limits12 invocations/24 model requests/32 tool calls/
65536 reported tokens/600 active seconds. Role profiles remain8 requests/12 tools/
32768 tokens/120 seconds/max_retries1. Capture wall900 seconds and2MiB output;
these are not an invoice cap, a guaranteed pre-spend token cap or an unconditional
teardown deadline. There is no second attempt under this plan.

The existing public test initializes a disposable repository/state, configures
profiles, creates a code-change Run, approves bounded observed requests, and
inspects status/logs/artifacts. Candidate writes and real verification execute
within the existing isolated Docker provider. No original target patch is applied.

## Milestones

1. Pre-dispatch admission: accepted clean code/import/dependency/source holds,
   exact selection and wrapper comparison, new private output, same pinned image
   and daemon, independent plan review. No key before these checks pass.
2. Exactly one original live test. Record actual exit, stage, bindings, requests,
   tool/code evidence and unknown usage without relaxing any assertion.
3. After confirmed stop, independently review original evidence and scoped
   physical cleanup. Update documentation and deliver with `[skip ci]`.

## Detailed implementation steps

1. Root owns this plan and the new private wrapper only. Reuse existing
   `scripts/run_live_canary.py`, `tests/live/test_provider_smoke.py` and
   `tests/live_provider_support.py`; do not create a duplicate harness or test.
2. Preflight the new wrapper against05 by AST, permitting only output05->06 in
   both existing literals. Validate the existing accepted-head check, selection
   guard, no-echo input, minimal child environment and original CLI dispatch.
3. Root supplies the credential transiently and executes the original launcher
   once into `openai-mixed-live-06`. No writer/subagent may access that credential.
4. Wait for normal completion or original watchdog outcome. Preserve its original
   attempt, summary, canary-evidence, cleanup, safe logs and fixtures; do not
   delete or overwrite earlier or new evidence.
5. Fresh verifier admits only stopped, canonical, no-sidecar original journals;
   validates exact Run/task/patch/role/command/usage bindings and original bundle.
   Query only retained full native IDs and exact installation scope on the bound
   daemon. No verifier test replay, cleanup, DB mutation or provider access.
6. Root records actual results and remaining campaign/native/provider gaps;
   document-only Git delivery does not require rerunning unchanged full gates.
   README is a package input, so run the original distribution and three fresh
   installation gates again against final result-documentation bytes; freeze
   README/guide until both stop and independent package/install review completes.

## Validation plan

Predispatch source maps and module paths must match accepted H/root bytes.
Pinned local SDKs are openai-agents0.22.1, openai3.8.0, pydantic2.13.5,
pydantic-ai-slim2.39.0 and langgraph1.2.11. Compare actual installed distribution
versions against the accepted environment before dispatch.

Local daemon checked2026-09-12T16:29:28Z:
e470601e-5d84-43d4-8c9d-2038812fa634, Linux/aarch64; standard image
SHA2562afebd51ae66f07096063b53fc14b6a45b18dd63b01d7f31ed0cb640c51da241,
Linux/arm64. No pull or build is authorized for preparation.

The exact original case is
`tests/live/test_provider_smoke.py::test_live_provider_cli_cos_engineer_verifier_canary`.
Require all original assertions: CoS/Engineer/Verifier completion in order with
exact selections, ready_for_review/verified_complete, expected one-file patch,
independent passing Docker commands, no proof gaps, unchanged target, complete
finite usage and no outstanding leases. Launcher PASS additionally requires
normal exit, assertions_passed, cleanup_complete and no diagnostic errors.
Independent readback must agree; pytest exit0 alone is insufficient.

## Rollback and recovery

No product edit/migration or target patch application. Never replay live05 or06,
refund reported usage, erase UNKNOWN, or promote a failed bundle after cleanup.
Original synchronous finalizer owns exact cleanup. Signal/timeout/uncertain owner
or missing finalization withholds recovery; stop and report retained facts rather
than broadly removing resources. No new native P0 write implementation is allowed.
Failure ends this attempt; a future decision is a separate bounded task/plan.

## Progress

- [x] (2026-09-12 16:29 UTC) H is accepted/merged and remote readback confirmed;
  clean launching root, all556 held inputs and129 postmerge contracts confirmed.
- [x] Created this new plan before dispatch, after actual H Git delivery.
- [x] Independent structural preflight PASS released16:34:28.035062 UTC;
  verdict SHA256f5a4405dffdc287b5dccb08b11e87bfafe348e4604748ce4d89ceb2fe955c812.
  Reviewed pre-dispatch plan SHA256
  6fd136e6b7264eb1bafa1904468de963887c87017537c3705a79a7b31eef4d74 and exact
  wrapper06 SHA25624e4db8a9bbad9009807444e467e47d96b21a51a3bf3c487d168fe48e219ee8d.
  Exactly two output-name substitutions from preserved05; source/selection/new
  output/guards/limits unchanged, no failed oracle or launch. Root additionally
  compared all96 installed distribution versions against accepted H environment:
  identical, no duplicates/differences. Imports,556 inputs, pinned image/daemon
  and clean accepted HEAD hold. These metadata entries precede credential input.
- [x] One original attempt stopped normally:2026-09-12T16:36:47.936976Z to
  16:37:23.899443Z,35.962s. Launcher exit1/NOT_PASSED; pytest1 failed/34.14s,
  13 dependency deprecation warnings; no retry or target patch application.
  Original summary reports cleanup_complete=true, recovery_withheld=false,
  diagnostic_errors=[]. Root released stopped evidence for independent review.
- [x] Original safe records identify failed target
  run_9c678a1c3a904040a9223b52ae69e14e, COMMAND_DENIED, changed_paths=[], message
  "Canonical candidate changes are empty or outside the TaskSpec path scope."
  The Engineer's persisted ImplementationReport passed structural validation,
  but claimed an edit and passing tests that did not occur. Its sole tool produced
  a real Docker python-test receipt with exit1 and empty-patch SHA256
  e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855.
  No canonical patch or independent Verifier result exists. Original bundle
  remains inconclusive with cleanup_unproven; standalone command artifact is
  not a Run-attached accepted result. Later cleanup must not promote it.
- [x] Reported accounting: CoS1 request/4686 input+3054 output=7740 tokens;
  Engineer2 requests/5018 input+203 output=5221 tokens; total3 requests/12961
  tokens,2 invocations,1 tool,23.221317 active seconds. Unknown/reserved/outstanding
  requests/tokens0; reported_costs={}, actual charge unknown. These values do not
  mean zero cost. The uninvoked Verifier remains unqualified.
- [x] After dispatch stopped, root copied the preregistered plan unchanged into
  the accepted H environment's new docs-only result branch, then added actual
  outcome bookkeeping. Original separate registration worktree/draft and verifier
  pre-dispatch hashes remain retained. Executed ROOT stays clean at PR16; no
  application or test source is changed. README now reports06 instead of05;
  original H full acceptance remains source-qualified, with fresh package/install
  checks required for new README bytes.
- [x] Independent stopped-original audit PASS released16:48:22.612139 UTC;
  original live outcome remains NOT_PASSED. Verdict SHA256
  9b9ffa77aee04107fce49debcdb6999c3120de08a0962b3a44ee023471d4edca;
  machine result a5b9aacc34eb0d16c3e2c2748ca17673ade16194341fe85a0d026fce75910f4b.
  All38 artifacts hash/size-bound:26 Run-exported and12 separate project entries.
  Original report, failed command, false success claims, three requests/12961
  tokens and no write/Verifier events independently agree. Both original bundles
  replay through pure CompletionGate: target inconclusive/verified=false with
  all five criteria inconclusive and original cleanup=false; separate bootstrap
  remains verified. Original five tracked target files/HEAD and145 fixture-file
  hashes plus five safe captures stayed unchanged.
  All9 leases released;3 exact workspaces and native IDs absent, exact installation
  44012bc4304145e8ddb1b0e5f4068b36 empty on recomputed matching daemon. Canonical
  owned single-link journal admitted O_NOFOLLOW/no handles/no sidecars and read
  immutable; its958464 bytes/hash remained unchanged. No global cleanup claim.
  Two private postprocessor errors were preserved: hashing a safe projection as
  a full configuration, then using a public field name on typed Run. Corrected
  readback against identical evidence passed; both stopped before native queries.
  A nonexistent optional source-search path is also retained. No trial/model/test
  retry, original evidence mutation or cleanup by verifier. The failed trial did
  not reach final whole-tree exact-secret-form assertions, and the keyless verifier
  does not claim a new exact-secret scan; no raw provider exceptions were read.
- [x] Final-README original distribution gate stopped exit0:1 test, zero
  failure/error/skip, wrapper5.763942957s and556 unchanged current inputs;
  JUnit SHA256088a1ec736c059ed7ce917af2b889571aa19cab3779273b545683fe73ed76d54.
  Original three-case fresh-install gate also stopped exit0, zero failure/error/
  skip, wrapper281.632112980s,556 unchanged inputs; JUnit SHA256
  5e15e1dcf8ada9770d7aa05db4f1573806b3eb370ef0a771c256337ebcf0e1da.
  Both use the unchanged `verify_optional_slice.py` entry points recorded in
  the H plan, modes package/installed, new live06-results-package-01 and
  live06-results-installed-01 outputs, live disabled and prepared offline assets.
- [x] Final-README package independent PASS released16:47:44 UTC; verdict
  SHA2565a705ab27ede9768ccf4992da288dba00957ef15e6a11607146210b6193eadbf.
  Original JUnit3.578s; both archives match332 current runtime/resources,
  115 schemas,13 migrations,5 prompts and34 dependency declarations. Current
  README SHA256f3c22076b75f9b39b031a50f02a1e4a110ea4860b0178c337e463d05a8791350;
  wheel0a71146f91c268e43f2e448d8550b1a40b82d39fb3fecccb5b703e9315a74c9d,
  sdist44c1e4df14af739de5e495b78eecd9310f87cb5d1e7aef4b797de9cef5d144bc.
  All556 current inputs,341 artifact identities and392 entry metadata records
  unchanged. Only README differs from H packaging inputs. No test rerun, DB
  content read, resource mutation or failed oracle in package review.
- [x] Final-README installation independent PASS released16:53:03 UTC;
  verdict SHA256df28d432c7999141f93c4245c4f4012b0fec6184a5ad680447394efb47f3a5fe.
  Original three-case pytest279.48s. Six archives equal the current accepted
  pair; three installs each match332 resources/115 schemas,80 distribution
  metadata entries and exact guards/assets/bundles. Six immutable migration1–13
  journals,215 artifacts,10 CompletionGate decisions and18 receipts validate.
  Fake negative/replay cases remain distinct from public real-Docker target
  run_d77449be9659442e9bc95fd86e15f7ac, whose Engineer and Verifier each retain
  actual5-passed/exit0 receipts, empty proof gaps and explicit one-file apply.
  All54 leases released,18 workspaces and6 exact native IDs absent in the sole
  queried installation feb6f0585b114f779fb64669dfc6f0b9. All556 current inputs
  and62291 retained entry metadata records unchanged; original PID2817 absent.
  No test/CLI/provider/recovery replay, original evidence mutation or deletion.
- [x] Initial final-doc preflight released16:54:46.654862 UTC with one FAIL:
  H plan's old unqualified "no new paid request" wording was ambiguous after06.
  Retained verdict SHA2563bc8b80a7cae8ff2c14ba82a22efb2b2d4482df06623cea3022dbcab56bb5584.
  All other scope/link/hash/sensitive-pattern and evidence checks passed. Root
  applied only the agreed H-slice chronology qualifier plus actual installed
  acceptance bookkeeping; README/guide/source remain frozen. No test rerun.
- [x] Corrected final-doc preflight PASS/read release16:58:31.901972 UTC;
  verdict SHA2568a0a23398dd89950af29d28c1d133a3ea7d0c441143d44f59be43bf52ea82f8d.
  Reverse-hash comparison establishes only the agreed chronology qualifier,
  released installation results and retained initial-FAIL bookkeeping changed.
  Exact four-document scope, clean index/whitespace, all554 source inputs,
  current README and guide remain unchanged. No test/resource/model replay.
- [ ] Exact documentation results and scoped commit/push/merge/readback.

## Discoveries

- H establishes an explicit strict Engineer wire contract and fixed parser-stage
  diagnostic, not a proven explanation for live05's missing raw payload.
- Prior failure/tool accounting is valuable evidence; successful subordinate
  commands do not establish accepted Engineer output or final delivery.
- This attempt demonstrates why structural validity cannot establish truth:
  the report claims success while the canonical patch is empty and the actual
  command failed. The control plane rejects that delivery before a Verifier.
  It does not establish why the model made those unsupported claims or make
  all previous schema failures explained.

## Decision Log

- September12, after PR16 delivery: root explicitly chooses one new attempt
  under the owner's renewed key/test authorization. Hold selection, prompt,
  test and limits fixed so the new observation concerns accepted H source.
  No automatic retry, spend escalation or missing-provider substitution.

## Outcomes

Live06 is NOT_PASSED with the bounded observations above. No second attempt is
authorized by this plan. Independent original-evidence/physical readback passed;
final package/installation acceptance and corrected final documentation readback
passed. Scoped Git delivery remains pending at this pre-publication record.
Native P0,24-task/cold-start and broader Provider/Harness
campaigns remain unqualified. H's strict terminal contract has one observed
schema-valid real submission, not successful mixed-Harness task qualification.
