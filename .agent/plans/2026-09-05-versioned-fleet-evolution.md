# Phase 6: reviewable, versioned Fleet evolution

This ExecPlan is a living document. Maintain Progress, Discoveries, Decision Log and Outcomes. It implements Milestone 4 of `2026-09-05-mvp-completion.md`. This initial version was prepared during Phase 5 final gates, which have now passed. Phase 6 source implementation proceeds under the accepted Phase 5 checkpoint and frozen slice contracts below; joint application and release acceptance remain open.

## Purpose and user-visible result

A user tells CoS: “For backend changes, always run integration tests.” CoS produces a validated FleetPatch proposal with semantic and textual diffs; the organization remains unchanged. Explicit user application creates an audited configuration version. A later backend task actually requires the exact integration command in its TaskSpec and independent evidence. Rollback is a new reviewed inverse change, not deletion of history.

```text
fleet chat .
> For backend changes, always run integration tests.
fleet fleet-patch diff <proposal-id>
fleet fleet-patch apply <proposal-id>
fleet fleet-patch rollback <applied-proposal-id>
```

Rollback is itself the user's explicit authorization for an exact current-head inverse. It persists a new inverse proposal and a new application operation, then restores through the same reviewed publication protocol. The returned new IDs/diffs remain inspectable; history is never erased.

## Scope

### In scope

- Reuse the existing FleetPatch wire format, IDs, path/base/content/secret validators; add missing aggregate/ancestry constraints.
- Natural-language CoS proposal delivery through the ordinary bounded Run/conversation, with no Engineer, worktree, direct repository write or tool-authorized application.
- Typed referenced workflow definitions and declarative verification skills whose path-conditioned requirements are actually consumed by TaskSpec/verification/evidence construction.
- Complete bounded organization-tree manifests, staged validation, durable immutable proposals/versions, exact semantic/text diffs, explicit apply and inverse rollback.
- Project-wide mutation fencing, atomic same-filesystem whole-directory exchange, exact recovery journal and Project configuration rebinding.
- CLI list/show/diff/apply/rollback, explicit stopped-owner recovery if publication outcome is uncertain, schema/migration/security/behavioral tests and documentation.

### Out of scope

- Changing `.fleet/fleet.yaml`, user trust, credentials, grants, approval ownership, hard denies, sandbox/provider/runtime ceilings or audit history through FleetPatch.
- Arbitrary new roles/topologies, executable skill scripts, shell/network expansion, remote filesystem/daemon trust, a hosted registry or automatic evolution/application.
- Applying arbitrary old rollback snapshots over newer versions. The minimum rollback targets the current applied version only and fails on drift.
- Phase 7 packaging/complete guide/platform release proof and live-provider/license prerequisites. No actual credential will be discovered or selected.

## Current repository state

Phase 0–5 is accepted and pushed through `a46b68800fb9725274db869f8c360b97a70c24ef`, tree `d99b632814b55d2a95b13e706b87d2017578c827`; remote main remains `c700de1`. Chat adds atomic conversation/Run ownership and migration 7. The full default suite passed 1472 tests, thirteen real-Docker cases passed separately, and metadata advanced to 5 only after those gates. The persistent-chat plan records exact evidence and post-marker refreshes. Phase 6 source starts only under an explicitly frozen slice contract below.

`domain/fleet_patch.py` currently validates the FleetPatch wire schema, exact file content hashes, base hash and allowed organization paths, including pre-parse/whole-proposal registered-secret checks. It has no operational persistence or CLI. `adapters/config/yaml.py` validates FleetSpec and VerificationProfile, snapshots referenced files and publishes only a new nonexisting `.fleet` tree. Referenced workflow YAML is currently snapshotted but not parsed as an executable typed definition. No operational skill model exists. `WorkflowEngine._scope` freezes verification commands directly from VerificationProfile. Thus editing workflow prose alone cannot implement the backend requirement.

ConfigSnapshot deliberately covers only FleetSpec and referenced files. README and unreferenced safe files are outside that logical hash. A directory replacement must also bind and preserve the complete bounded tree, or it could silently lose user content. `SqliteStateStore.save_project` is an unconditional upsert, so application evolution also needs a revision/fence transaction, not a racy list-of-running-Runs check.

## Security impact

All AGENTS and SECURITY_MODEL invariants remain. CoS output is untrusted data, including proposal IDs, hashes, rationale and claims of validation. The control plane preallocates and checks expected project/proposal/base identities, rebuilds the prospective tree, derives diffs and validates supported runtime semantics. Tool catalogs expose neither apply nor permission mutation. Adding a required command never grants permission to execute it; current Broker/Grant/dispatch rules still apply, including separate Engineer/Verifier identities.

Proposal persistence and filesystem publication are separate boundaries. All saved payloads/artifacts/errors are bounded and registered-secret scanned. Safe unreferenced files are preserved byte-for-byte; symlinks, hardlinks, special files, case aliases, path ancestry conflicts, invalid UTF-8, protected/sensitive tree entries and bounds violations fail before staging/publication. `.fleet` remains a real directory, not a pointer symlink. The local OS account/filesystem remain trusted; no cross-account tamper-proof claim is made.

An exact project fence must participate transactionally in Run registration (ordinary and chat), configuration-dependent continuation, initialization/rebinding and target code-patch apply. A caller whose preflight used an old configuration must fail after an intervening version change. The fence does not expire automatically. Recovery requires confirmation that the original publisher stopped and never overwrites an unexpected target.

Admission must bind a monotonic head revision and complete tree identity, not only the logical config hash: README-only changes can preserve that hash and rollback can repeat it. Freeze the exact legacy/no-head compatibility contract before changing existing Run/conversation/graph binding serialization. Blockers include CREATED, running, paused/child-wait states, retained claims and outstanding descendant/failed leases. Ready-for-review candidates from an older organization version must not apply even if rollback later repeats their logical hash.

## Proposed design

### Typed configuration and effective behavior

Preserve the existing valid generated Workflow shape and top-level FleetSpec ceilings. Add strict referenced code-change workflow/verification-skill schemas; exact field names and defaults are frozen before their writer begins. Skills are declarative requirements over canonical repository path prefixes and reviewed verification command IDs, not scripts or new authorization.

For a code-change TaskSpec, union baseline required commands with requirements whose path scope intersects the allowed candidate scope. Broad parent scope also intersects a narrower backend prefix; CoS cannot evade a requirement with a broader scope or by omitting a command from its output. All referenced commands must exist, match the supported canonical command surface and retain `networkRequired=false`. Direct read-only responses do not run behavioral commands. Parent joined verification covers the original complete requirements; child scope may narrow work but cannot relax final parent evidence. Verifier mutation immunity remains unconditional trusted code, never a configurable prompt choice.

Prospective whole-tree validation checks schema/reference closure, unique names, size/encoding constraints, runtime capabilities and unchanged protected settings. Referenced skills join ConfigSnapshot using deterministic ordering; existing snapshots without skills retain their exact serialization/hash behavior. Semantically malformed workflow YAML that used to be merely opaque is rejected with a migration/compatibility explanation, not silently accepted.

The narrow wire direction is `WorkflowDefinition.verificationSkills` referencing canonical `skills/*.yaml` beneath `.fleet`, and `VerificationSkill` with version/kind/name, `appliesToPaths` and `requiredCommandIds`. Explicitly extend the existing FleetPatch allowlist only for this validated declarative skill type. Reject executable skill payloads, duplicate YAML keys, dangling/undeclared skill references and inconsistent duplicate workflow limits; no generic configuration loading of arbitrary plugin content.

### Proposal delivery through CoS

The preferred narrow output seam is `ScopeDecision | FleetPatch` for CoS only; other role output schemas stay unchanged. Keep the existing FleetPatch serialization and validator import API. Resolve the current models/fleet_patch dependency cycle by moving only pure schema declarations to a shared non-cyclic location or to models with re-exports, not inventing another mutation format.

The trusted invocation supplies a preallocated proposal ID/project/base and bounded current organization context. `_scope` recognizes a validated FleetPatch and builds a direct/read-only proposal-delivery Task/Plan. It persists recomputed proposal/snapshot/manifest/diff evidence and a bounded CoS response, then completes that turn without applying files. Neither the model's selected strategy nor prose can invoke a publisher. The same cumulative accounting, exact conversation owner and secret checks govern proposal calls. Oversized or incomplete context is explicit; a proposal cannot change a file whose exact before identity the control plane cannot establish.

### Durable version journal

Migration 8 is expected to add four records: proposals, immutable configuration versions, project head/fence, and publication operations. Exact Pydantic shapes, authority reads and method signatures will be frozen before implementation.

- Proposal: original FleetPatch and hash; exact project/repository/source Run; base version; before/after logical ConfigSnapshot and complete tree manifest refs; semantic/text diff refs; validator version.
- Version: `(project_id, version)`, predecessor, config/tree hashes, exact artifact refs and originating operation.
- Head: revision, active version and optional pending operation.
- Operation: exact claim/proposal/head revision, before/after identities, explicit user-authorization receipt, status/timestamps and append-only audit binding.

Operation states are PREPARED, COMMITTED, ABORTED and RECOVERY_REQUIRED. An unknown operation is not replayable. First observed version registration records a baseline, not invented historical user approvals. Read APIs validate SQL/JSON identity, immutable receipts and exact artifact bytes before reporting an applicable proposal. Run creation cannot bypass a pending head/fence through a legacy no-chat helper.

### Atomic publication and recovery

1. Read the complete bounded current tree; prepare exact prospective contents and logical snapshots. Stage them in a descriptor-validated private `0700` scratch directory beside the repository, outside the repository itself, on the same filesystem.
2. Fail before publication if scratch ownership/privacy, filesystem identity, directory handles, native atomic exchange or required durability operations are unavailable. Never fall back to multi-file overwrite or an exposed staging tree inside the repository.
3. Acquire the exact repository publication lock. In SQLite `BEGIN IMMEDIATE`, recheck Project/config/head and outstanding active/paused/owned work/resources, acquire the project fence and record prepared audit before filesystem mutation.
4. Revalidate pinned before/after trees and atomically exchange the two real directories using the supported macOS/Linux same-filesystem primitive. Fsync exact affected directories. The prior tree becomes the owned backup.
5. One SQLite transaction records the new immutable version, CAS-updates Project configuration bindings, advances the head, records completed audit and releases the fence. Clean only exact owned scratch after authoritative commit read-back.

Before exchange, staged file contents, complete manifest and exact owned directory metadata must have their supported durability operations completed. Freeze supported file/directory modes and both before/after pinned identities. Persist content-addressed before and after bytes for every whole-tree file, including README/unreferenced content, so rollback works after the temporary backup is removed. Hash-only manifests or a textual diff cannot reconstruct an exact inverse.

Publication must also rebind the exact Fleet-owned Git status allowance: otherwise tracked `.fleet` changes can cause the next promised task to fail PROJECT_DIRTY. Recompute only the reviewed organization delta while proving unchanged source/index/HEAD and unrelated user status. Test both initially untracked generated `.fleet` and tracked organization files. Do not broaden the ordinary dirty-worktree allowance or rewrite old Run evidence.

SQLite and filesystem cannot form one transaction; the journal/fence makes the intervening state unavailable for execution and explicitly recoverable. PREPARED plus exact unchanged target can abort. Exact after-target plus exact before-backup can be explicitly reconciled after owner-stopped confirmation. COMMITTED plus exact after-target returns the original result without a second exchange. Any unknown hash/owner/target/backup retains the fence and reports RECOVERY_REQUIRED. A caught pre-commit failure may restore only when both trees still match and commit failure is known; uncertain commit outcome cannot trigger an automatic reversal. Recovery never cleans a broad sibling directory or resets the repository.

Rollback validates that the selected applied proposal is still the current head, reconstructs exact prior bytes as a new inverse FleetPatch linked by `rollback_of`, persists new diff evidence and applies it under that explicit user command. Drift/newer versions fail; the inverse uses the same full validation and publication protocol. An optional preview may persist/show the inverse without applying it, but bare rollback must not report restoration when only a proposal was created.

## Public contracts

- `fleet fleet-patch list|show|diff|apply|rollback`, selected project where required, stable JSON envelopes and typed conflict/recovery errors. Apply always consumes one exact persisted immutable proposal, not a model-provided filesystem path. Rollback creates and applies an exact inverse through a new audited operation under explicit user authorization.
- CoS output union preserves the existing ScopeDecision tool name and role contracts; add one named FleetPatch output without exposing application tools.
- Strict workflow/verification-skill schemas and deterministic effective requirement calculation. No protected FleetSpec or trust-schema expansion.
- Typed manifest/proposal/version/head/claim/operation/publication receipts; migration 8 with forward-only compatibility.
- Exact event identities for proposed/prepared/applied/aborted/recovery/rollback proposal; all append-only and redacted.
- Precise field/method names, transaction guards, filesystem port and single-writer ownership remain **design pending**, not delegated source contracts yet.

## Milestones

### Frozen slice A contract: executable configuration requirements

Authorized only after accepted Phase 5 checkpoint `a46b688`. The configuration writer owns `domain/config.py`, `adapters/config/yaml.py`, `ports/config.py`, one new `tests/unit/test_verification_skills.py`, and directly related existing config tests. Root owns WorkflowEngine/EvidenceAssembler consumption, FleetPatch models/validator, runtime integration, schemas and all other source. The writer must preserve other edits and report rather than modify overlapping workflow/evidence/schema files.

`WorkflowDefinition` retains existing `apiVersion`, `kind: Workflow`, Metadata, exact generated stage sequence and `limits.maxRepairIterations` (0–5), and adds optional `verificationSkills` (default empty, at most 32 unique canonical `skills/<name>.yaml` references). `VerificationSkill` uses the same API version, `kind: VerificationSkill`, Metadata, `appliesToPaths` (1–128 unique canonical non-protected repository prefixes) and `requiredCommandIds` (1–128 unique ActionIds). The supported skill is declarative only; unknown fields, executable hooks and authority settings fail. Metadata/workflow reference names must agree; required command IDs must exist in the validated profile. Duplicate workflow repair limits must agree with the corresponding FleetSpec workflow ceiling; inconsistent files fail rather than silently choosing a value. No new role/topology or mutable stage order is introduced.

Add `ConfigurationPort.snapshot_from_files(files) -> tuple[FleetSpec, ConfigSnapshot]` and `required_verification_commands(spec, snapshot, *, workflow_id: str, allowed_paths: tuple[str, ...], change_kind: Literal["read_only", "code_change"]) -> tuple[str, ...]`. YamlConfigurationAdapter implements both with the same validators used by filesystem loading. For code changes, the latter returns sorted unique baseline-plus-applicable-skill command IDs using component-wise path overlap; broad `.` scope matches every prefix. Read-only tasks return no required execution commands. Root freezes this result in TaskSpec and recomputes it from authoritative config/scope at evidence assembly.

Loading, in-memory prospective validation and snapshot construction all parse referenced workflows and their skill reference closure; preserve exact raw file bytes and deterministic sorted snapshot ordering. Existing valid no-skill configurations retain byte-identical ConfigSnapshot serialization/hash. The generated default files must remain unchanged when no skill is selected. YAML duplicate keys, alias/anchor abuse, malformed UTF-8, secret-bearing values, missing/aliased/unsafe paths, unsupported stage changes, duplicate names/references and missing commands fail before trust or file writes. Reuse existing 512,000-byte file, 4,000,000-byte aggregate and guidance bounds; bounded reference traversal must not introduce unbounded filesystem discovery.

Acceptance for slice A: valid old defaults round-trip/hash identically; a seeded backend skill survives snapshot/reopen and makes integration mandatory for backend or broad scope but not disjoint frontend/read-only scope; invalid/protected references and YAML fail closed. Tests assert effective frozen task requirements once root integration lands. Whole Phase 6 publication/store/CLI contracts remain pending and are not authorized by this slice.

### Frozen slice B contract: canonical FleetPatch schema boundary

The FleetPatch writer owns `domain/fleet_patch.py`, ONLY the new pure FleetPatch declaration/import hunks in `domain/models.py`, and `tests/unit/test_fleet_patch.py` plus one narrowly named new validator test file if useful. No other models.py declarations, RuntimeOutput union, adapters, schemas or source files may change in this slice. Root will wire the CoS output union only after these declarations freeze. Writers are not alone and must preserve other changes.

Move/re-export the existing FleetPatchOperation, FleetPatchFileChange, FleetPatch and their constrained field aliases into canonical models.py so the later CoS output union has no import cycle. Preserve existing public imports from domain/fleet_patch.py, field names/aliases/kinds/IDs and valid serialized wire output. Do not introduce another mutation format or operational apply behavior. Tighten canonical path validation against ASCII controls/DEL and case-folded file/ancestor collisions. Extend the organization-path allowlist only to canonical `.fleet/skills/<name>.yaml`; resulting skill schema/reference validity remains mandatory in whole-configuration validation, not authority inferred from a path.

Before Pydantic/expensive whole-proposal processing, bound plain built-in JSON tree depth/nodes and total UTF-8 string content to 4,000,000 bytes, reject malformed UTF-8 and cycles/subclasses, then perform the existing registered-secret scan. Typed validation repeats whole-proposal scanning/bounds and current base/hash/path checks. Existing per-content wire constraints remain; prospective configuration additionally enforces its tighter per-file/aggregate ceilings. Errors must be typed/cause-free at the untrusted parser boundary without echoing raw invalid input. Preserve exact valid proposal serialization and add negative tests for aggregate bounds, malformed UTF-8, controls, ancestor aliases, protected settings, registered secrets and canonical skills. No persistence, publication, schema regeneration or source effects outside this boundary are authorized by slice B.

### Frozen slice C contract: bounded real-directory publication primitive

The publication writer owns only new `domain/organization_tree.py`, `ports/organization_filesystem.py`, `adapters/config/publication.py`, and narrowly named unit/contract tests for those modules. It does not change existing models, persistence, artifacts, application, CLI or schemas. Other writers work concurrently; preserve their changes. This adapter implements filesystem mechanics, not user approval or database head decisions.

Use frozen strict domain records: `OrganizationFile(path, content, sha256, mode)`, `OrganizationDirectory(path, mode)`, `OrganizationTree(files, directories)` with a deterministic `sha256` property; `DirectoryIdentity(device, inode, uid, mode)`; `PreparedPublication` and `PublicationObservation` binding the exact operation/project/repository identity, pinned parent/repository/target/scratch/staged directory identities and before/after tree hashes. Operation IDs use `fop_<32 lowercase hex>`. File paths are relative beneath `.fleet`; directories include exactly one `.` root. Preserve safe unreferenced UTF-8 files and empty directories. Files are sorted/unique and bounded to 256 entries, 512,000 UTF-8 bytes each, 4,000,000 total. Directories are sorted/unique, at most 256 including root, at most 16 components deep. Require complete parent-directory closure, reject file/ancestor/casefold collisions, controls/DEL, traversal, malformed UTF-8, sensitive credential/VCS entries and registered secrets. Permit only non-executable regular file modes `0600` or `0644`, directory modes `0700` or `0755`, owned by the current OS user. Reject symlinks, hardlinks, special files and unsafe modes rather than normalizing them silently.

`OrganizationFileSystem.session(project: Project, operation_id: str)` returns a context-managed `OrganizationPublicationSession`. The session provides `capture_target() -> OrganizationTree`, `stage(after: OrganizationTree) -> PreparedPublication`, `observe(prepared: PreparedPublication) -> PublicationObservation`, `exchange(prepared: PreparedPublication) -> PublicationObservation`, and `cleanup_owned(prepared: PreparedPublication, *, expected_target_sha256: str, expected_backup_sha256: str | None) -> None`. The application invokes exchange only after durable exact journal/fence preparation; this internal port is never available to a Harness or tool catalog. Reopening with the same exact prepared receipt supports inspection/recovery; no implicit exchange or cleanup occurs on context-manager exit. Cleanup is explicitly authorized by the application after authoritative commit/abort read-back and validates exact owned identities/trees; it never removes unexpected content.

Acquire a stable advisory publication lock outside the repository using a deterministic name derived from its canonical identity in the repository parent; validate owner, regular-file identity, single link and mode `0600`. This serializes different state-directory clients against the same repository, unlike a state-local-only lock. Fail promptly if another session owns the lock. Use descriptor-relative traversal and pin the repository/parent chain; do not follow mutable symlinks. Create an exact private `0700` scratch sibling outside the repository on the same filesystem, with a single real `tree` directory. Stage becomes backup in place after native exchange; no third rename. Receipt records the exact scratch basename and identities, not an arbitrary user-supplied path.

Use macOS descriptor-relative `renameatx_np` with `RENAME_SWAP=0x2`, or Linux libc `renameat2` with `RENAME_EXCHANGE=0x2`; fail closed if unsupported. Verify and fsync staged contents and directories before returning prepared, revalidate both pinned trees before exchange, then fsync affected directories and inspect exact swapped identities/hashes. Use `F_FULLFSYNC` for regular-file durability on macOS where supported; record backend/durability explicitly. Fail before exchange on unsupported required staging flushes. Any failure after exchange must retain receipt/scratch and surface recovery-required, never guessed reversal or cleanup. No syscall-number guessing, pointer symlinks or multi-file fallback.

Durability is a supported local OS/process-crash protocol, not a guarantee against broken hardware or ignored drive-cache flushes. macOS documentation explicitly limits ordinary fsync; Linux filesystems may reject rename flags. The later journal writer must use explicit synchronous/fullfsync settings and persist immutable content-addressed complete tree bytes in its durable database before exchange. Existing ArtifactStore read-back alone is not treated as durable rollback storage because its parent directories are not fsynced. This choice avoids expanding that accepted artifact API merely for publication recovery. Recovery also depends on the local filesystem honoring its documented operations; unsupported environments fail closed.

Acceptance: real temporary-directory capture/stage/exchange/reopen/cleanup on the host; exact modes/unreferenced bytes/empty directories preserved; unsupported backend, descriptor/path/inode/hash/ownership drift, links, UTF-8/bounds/secrets, lock conflict and injected before/after-exchange failures fail closed. Tests retain exact receipts and real filesystem observations, not only syscall mock counts. Cross-platform behavior is implemented and unit-tested but Linux runtime evidence remains a separate Phase 7 gate on an available Linux environment.

### Frozen slice D contract: durable version journal and transaction-local admission

The journal writer owns new `domain/evolution.py`, `ports/evolution.py`, `adapters/persistence/evolution.py`, migration `0008.sql`, ONLY the `SUPPORTED_SCHEMA_VERSION` constant hunk in existing `adapters/persistence/sqlite.py`, and narrowly named new journal contract/unit tests. Root owns all existing SQLite transaction hooks, conversation/graph hooks, service wiring, schema registration and migration-count fixture updates. Preserve all other changes. The writer may import slice C's frozen models but must not edit them; coordinate if the filesystem file is not yet present.

Keep existing Run, Project, conversation and graph wire serialization byte-identical. Add a separate immutable Run admission table rather than new default fields in those models. `OrganizationAdmission(project_id, revision, tree_sha256, config_snapshot_sha256)` binds a monotonically increasing organization generation. `OrganizationHead` adds current Project canonical JSON hash and optional pending operation ID. Initial head/version is zero. `OrganizationVersion` binds project/version/predecessor, tree/config hashes, originating operation (null only for baseline), timestamp and its immutable audit receipt. Revision and version advance together on every apply/rollback, even when bytes or logical config SHA repeat.

`FleetPatchProposalRecord` contains the exact existing FleetPatch, source Run ID, base OrganizationAdmission, before/after tree and config hashes, recomputed bounded text diff and typed semantic changes, timestamp and immutable audit binding. `OrganizationOperation` binds operation ID, exact proposal/head/Project identity, PreparedPublication, authorization kind `apply` or `rollback`, status `prepared|committed|aborted|recovery_required`, timestamps and append-only audit receipt. Additional fields necessary for exact typed integrity are allowed; report signatures before implementation. No raw provider messages, credentials or unbounded JSON are stored.

SQLite tables cover content-addressed complete trees, immutable proposals, immutable versions, project heads, operations, immutable Run admissions, and append-only organization events. Complete tree bytes/modes/empty directories live in bounded content-addressed database records and are hash-validated on every load; recovery never depends on a temporary backup or unsynced artifact directory. Use explicit `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=FULL`, and on macOS `fullfsync=ON`, `checkpoint_fullfsync=ON`; verify effective settings. Before publication PREPARED commits, both before/after trees and all exact operation bindings must be durably committed. Read-back distinguishes a failed commit from uncertain outcome; never auto-reverse filesystem state here. Bounded plain JSON validation and registered-secret scans precede persistent encoding and after reads; SQL columns, JSON identity/hashes, record/event bindings and head/version ancestry must agree. Unknown/corrupt state fails closed. Internal events are append-only, with exact record hashes; no historical event rewrite for rollback.

Freeze `OrganizationStore` methods: `get_head(project_id) -> OrganizationHead | None`; `register_baseline(project, tree, config_snapshot_sha256) -> OrganizationHead`; `get_tree(tree_sha256) -> OrganizationTree`; `save_proposal(record, before, after) -> FleetPatchProposalRecord`; `get_proposal(proposal_id) -> FleetPatchProposalRecord`; `list_proposals(project_id, *, limit=50) -> tuple[FleetPatchProposalRecord, ...]`; `get_version(project_id, version) -> OrganizationVersion`; `prepare_operation(project, proposal_id, publication, *, authorization) -> OrganizationOperation`; `get_operation(operation_id) -> OrganizationOperation`; `commit_operation(operation_id, project, observation) -> OrganizationVersion`; `abort_operation(operation_id, observation) -> OrganizationOperation`; `require_recovery(operation_id) -> OrganizationOperation`; `admission_for_run(run_id) -> OrganizationAdmission | None`; `assert_current(admission) -> OrganizationHead`. Add `evolve_tree(before, patch) -> OrganizationTree` in the new pure domain module: preserve all unaffected bytes/modes/directories, preserve modes on updates, create files as `0600` and missing parent directories as `0700`, retain empty directories on deletion. No directory/mode changes can be smuggled through file changes. Validate existing FleetPatch before-hashes/after-hashes/protected paths and compare exact prospective tree; the adapter's injected ConfigurationPort validates both closures and recomputes logical hashes.

The concrete adapter is `SqliteOrganizationStore(database_path, clock, ids, redactor, state, config)`. It may call existing StateStore read validators and use its own transaction connection, but it must not bypass SQL/JSON/event verification. Baseline is idempotent only for the exact current Project/tree/config; it is not permission or invented historical approval. Proposal IDs are immutable/idempotent only for identical validated bytes. Source Run/project/config must match; rollback inverse records may reuse the original source Run as provenance but must link `rollback_of` to the exact currently applied proposal/version and current base. Prepare is idempotent only for the same exact operation/proposal/receipt; a different publisher sees the retained fence and cannot take over.

Prepare executes BEGIN IMMEDIATE and rechecks head/revision/tree/Project/config plus all execution blockers. Block project Runs in `created`, `running`, `waiting_for_children`, `paused_for_approval`, `applying`; all resource leases not `released`/`recovered`, including failed and descendant leases; active/fenced conversation claims and graph driver claims. Ready-for-review terminal candidates do not block evolution, but become stale for later code-patch apply. No TTL takeover. Commit requires the same pending operation, exact exchanged observation, and a new Project differing only in reviewed config hash/snapshot artifact, exact init status fingerprint and updated timestamp. Advance head/version and rebind Project in one transaction, record committed audit and clear fence. Abort requires exact pre-exchange observation. Recovery-required retains fence. Repeated committed calls return the original version without a new event/version/exchange; abort/commit crossovers fail. Filesystem ownership is checked by slice C/application, not inferred from a model receipt alone.

Export transaction-local helpers from the new persistence module for root wiring: `check_organization_admission(connection, run, admission, *, parent_run_id=None) -> None` and `record_organization_admission(connection, run, admission, *, parent_run_id=None) -> None`, plus `assert_organization_project_write(connection, project) -> None`. Helpers use already-open transaction; they never commit. With no organization head, no admission is required for legacy direct-store tests. Once a head exists, a newly registered root Run requires an exact non-pending admission. A graph child inherits its persisted parent's exact admission; a missing parent binding fails after a head exists. Root passes expected admission through ordinary/chat registration, acquires the cross-state repository session lock across final tree recheck and registration, and records it in the same transaction as Run insertion. Project writes after head creation accept only the identical current Project; reviewed evolution uses its private exact CAS commit, so legacy initialization cannot silently rebind a versioned project.

Legacy persisted Runs with no admission are executable/applicable only while a head is absent or remains baseline revision zero with matching logical configuration and ordinary target checks. After any applied version, all such legacy Runs fail continuation/application even if rollback repeats the old SHA. No old immutable Run/claim/graph hashes are rewritten. New Runs always get exact tree/generation admission through root service wiring. Current-only rollback creates a new inverse proposal and operation; no history deletion or replay of an old version number.

Acceptance: migration from schema7 preserves existing records; baseline/proposal persistence survives reopen with exact tree bytes; apply/abort/recovery transitions are immutable and idempotent, stale head/Project/receipt/schema/secret/JSON fail closed; every blocker including failed leases and retained claims is tested; two-connection admission/prepare barriers prove one winner; read corruption and unknown commit remain fenced. All publication observations in journal tests are explicit fixture receipts, not a claim that filesystem effects happened. Root integration later proves the real joint protocol.

### Milestone 1: executable configuration semantics and safe proposals

### Frozen slice E contract: admission hooks in existing transactions

After slice B, its writer owns ONLY admission-related import/signature/transaction hunks in `ports/state_store.py`, `ports/conversation.py`, `adapters/persistence/sqlite.py`, `adapters/persistence/conversations.py`, `adapters/persistence/graphs.py`, plus a new narrow admission-hook test file. The journal writer owns the schema-version constant in sqlite.py and new evolution modules; coordinate nonoverlapping hunks, never revert another writer. Root retains application/runtime/CLI/schema and other tests. No old Run/Project/claim/graph JSON fields or hashes change.

Add optional keyword-only `organization_admission: OrganizationAdmission | None = None` to StateStore.create_run, its canonical `_insert_run_in_transaction` helper, and ConversationStore.register_turn_run. Pass it through the conversation winner's Run insertion. Invoke slice D `check_organization_admission` before inserting a new Run, then `record_organization_admission` after insertion inside the same transaction. Duplicate conversation submissions retain the original immutable result and do not insert or rebind an admission; execution/resume remains separately gated by root's application service. Graph initialization checks and records each child's inherited parent admission around its existing direct Run insert in the same graph transaction; preserve existing graph identity/event/budget contracts. No headed root or child can enter through an unguarded legacy store path.

Guard `save_project` inside BEGIN IMMEDIATE using `assert_organization_project_write` before its existing upsert. No silent head/config overwrite while a publication is pending or after a versioned head exists. Do not add a generic bypass flag; the journal's reviewed commit performs its own exact CAS. Helpers never open or commit a second transaction. Tests prove normal/chat/graph registration participates in the same rollback unit, exact stale/missing admission fails, no partial Run/event/task/turn/claim remains on rejection, duplicate submissions do not rewrite old ownership, and identical legacy JSON hashes remain unchanged. Source evolution helpers may arrive concurrently; coordinate their frozen signatures and run tests once available.

Rollback refinement, frozen before journal implementation: `evolve_tree` also accepts keyword-only `rollback_target: OrganizationTree | None = None`. A file-only inverse cannot otherwise express removal of directories created by an earlier add or restoration of a removed file's original mode. Only a patch with `rollback_of` may supply this target. The store resolves and verifies the exact previous tree of the currently applied original proposal; it never trusts an arbitrary supplied historical snapshot. After validating inverse file operations, resulting file paths/content/hashes must equal that exact target. Only this explicitly authorized inverse may restore its recorded modes/empty-directory structure. Ordinary proposals preserve existing modes/directories and use fixed safe creation modes. This adds no new FleetPatch wire operation or model authority.

### Root integration contract: bounded CoS proposal context

Root owns `application/evolution_context.py` and application/runtime/service wiring. Supply at most 128,000 UTF-8 JSON bytes of organization context within the existing invocation ceiling. Include a trusted preallocated proposal ID, Project ID, logical config SHA and monotonic admission/tree identity. Prioritize workflow, verification profile and referenced skill content, with at most 32,768 UTF-8 bytes per visible file; report an explicit omitted-file count. Never include protected FleetSpec/trust/credential state. The model may propose adds through the existing allowlist, but may replace/remove only files whose complete contents were included. Reject model-provided rollback, mismatched proposal/project/base identities, unbounded content and registered secrets before proposal storage. Config/tree validation remains independent of model output and context selection. No application command or authorization tool is exposed through this output path.

### Cross-slice refinements frozen during initial integration

- All transaction-local admission/project helpers accept optional keyword `redactor`; production callers pass their active registry. Omission is allowed only when no head exists for legacy compatibility, never a headed execution fallback.
- `OrganizationOperation.repository_before` durably stores `OrganizationRepositoryBoundary(repository: RepositoryInfo, index_sha256, non_organization_status_sha256)`. `prepare_operation` requires keyword `repository_before`. Root's new repository port read compares exact staged entry/flag identity and non-Fleet status, repository identity and HEAD before/after publication and during recovery. Only the reviewed Fleet status allowance is rebound. No user index/source/HEAD write is authorized.
- Add `operation_for_proposal(proposal_id) -> OrganizationOperation | None`, with one immutable publication attempt per proposal. Committed repeats return the original version even after later evolution. Prepared/uncertain attempts require recovery; aborted proposals need a newly reviewed proposal rather than implicit retry.
- Add filesystem `sync_exchanged(prepared) -> PublicationObservation`: validate exact exchanged orientation and repeat only required durability operations, with no rename. Explicit stopped-owner recovery uses it after an exchange/fsync cut point; a read-only observation alone is not a durable completion receipt.
- Observed host fact: new macOS files/directories carry `com.apple.provenance`. Rejecting all extended attributes would make ordinary generated trees unsupported here. Preserve this one observed attribute as opaque exact bytes: `OrganizationXattr(name: Literal["com.apple.provenance"], value_base64)` with canonical encoding and at most 4096 decoded bytes; files/directories add optional `xattrs` tuple default empty, at most one. Capture never modifies a target. Stage removes inherited attributes only from freshly created owned objects, then restores recorded supported bytes and verifies them. Updates preserve prior attributes/modes; rollback restores the recorded complete target. All other xattrs, ACLs and special flags remain unsupported/fail-closed. Scan raw decodable and stored metadata for registered secrets before persistence. Attribute bytes affect complete-tree identity but never enter model context. This is exact preservation, not a claim about undocumented attribute semantics.
- Proposal delivery artifacts are separately bounded: serialized FleetPatch at most 8,000,000 UTF-8 bytes, textual diff at most its 12,000,000-byte domain ceiling. Structured proposal and diff use distinct ArtifactKind values and exact proposal hash metadata; they are not code patches and cannot enter ordinary code-patch apply.
- A further native probe returned success from `fremovexattr` while read-back still showed provenance. Empty metadata on newly created entries is therefore not reproducible under this host process. Refine deterministic creation policy: new files/directories inherit the exact recorded organization root's supported attribute tuple, keeping `0600`/`0700` modes; replacements retain their own prior attributes. Stage must reproduce and verify those exact bytes, otherwise fail before exchange. No privilege change, target normalization or guessed values. Empty-root metadata on Linux remains empty. Unreproducible/mixed metadata remains an explicit supported-filesystem limitation; an empty requested tuple cannot be silently changed to make a test pass.
- After completing admission hooks, the same writer owns only root's new `GitRepositoryAdapter.inspect_organization_boundary` method, narrow bounded-reader/import helpers, `domain/repository_boundary.py` refinements if needed, and a new dedicated repository-boundary contract test. Root does not concurrently edit these hunks. Stream/hash fixed secured Git output before retention, compare two complete index reads, bound status/config reads, enforce timeout and exact child cleanup; do not change ordinary inspect behavior in this slice.

### Milestone 1 acceptance

Final adapter refinements: the cross-state lock name binds the canonical repository root alone, not the mutable remote-derived Project identity. `cleanup_owned` additionally accepts `expected_backup_tree: OrganizationTree | None`; a complete immutable journal tree proves every remaining entry is an exact subset after interrupted cleanup, including an empty scratch after the backup directory was removed. Reject unexpected entries, metadata or pinned directory identities. Without a prepared receipt, staging failure retains private scratch and has no automatic retry/cleanup authority. Historical fenced conversation claims cease to block only when exact terminal reconciliation and latest binding/audit/generation are validated in the same journal transaction; active or unreconciled ownership still blocks.

### Frozen slice F contract: pure CoS content hashing

The completed admission/repository writer next owns only new `application/proposal_tools.py` and `tests/unit/test_proposal_tools.py`. Root owns all wiring, runtime/prompt/schema changes. A model cannot reliably calculate a cryptographic digest from text; requiring guessed SHA-256 values would make otherwise valid real-provider FleetPatch proposals unusable. Provide exactly one pure runtime utility, `fleet_content_sha256`, accepting only bounded UTF-8 `content` and returning its SHA-256/byte size. It has no filesystem, repository, process, network, credential, state, Artifact or approval access and no side effects. This is computation on already model-provided text, not resource authorization, and cannot replace Gateway/Broker checks for any effectful operation.

Implement `ProposalHashToolCatalog(redactor, *, max_calls=32)` with the existing RuntimeToolCatalog protocol. Definition has `side_effect=False`; arguments use strict plain built-in JSON and content at most 65,536 UTF-8 bytes (also obey existing RuntimeToolCall JSON bounds). Scan registered secrets before hashing/return/history retention. Result never echoes content; records contain only call ID/name/outcome, both side-effect flags false and no artifacts. Exact repeated call ID/arguments returns the original result without extra local computation budget; changed identity/payload fails. Never store raw content in completed-call cache: retain its canonical request hash and bounded result only. Recheck fresh registered secrets on every invocation, including repeats. Reject unknown names, subclasses/model_construct drift, malformed UTF-8, extra fields and excessive calls with cause-free typed errors and no raw input. Overall Run/tool budgets remain charged by the existing PydanticAI accounting wrapper.

Root will expose this catalog only to a CoS invocation with the trusted organization context; other role tools and all publication authorization remain unchanged. Update the prompt to instruct hashing proposed complete content with this tool before returning FleetPatch. Tests prove exact UTF-8 bytes, bounds, fresh-secret rejection, repeat/collision behavior, no content retention in records/cache/result, and no I/O. A later offline FunctionModel integration must actually use the hashing tool and then deliver a validated proposal; no real-provider success is inferred from that test.

Acceptance: a normal offline CoS call produces the existing FleetPatch format; resulting schemas/references/requirements validate; semantic and text diffs survive reopen; original tree/trust remain unchanged. A malformed/protected/secret-bearing proposal fails before authority-bearing persistence. Required backend commands are deterministic configuration semantics, not prompt wording.

### Milestone 2: exact version publication and inverse rollback

Root's joint application contract: expose typed `OrganizationPublicationResult` with exact proposal/operation/version, `changed` for this invocation, and nullable `cleanup_complete` (null means not inspected, not success). A committed repeat is read-only and returns the original version even after newer evolution; it never guesses whether historical scratch was cleaned. First application stages before durable prepare, rechecks the exact Git boundary, then exchanges and commits. If prepare fails and authoritative re-read proves no operation/fence exists, clean only the exact locally prepared scratch and report the failure. Unknown outcomes retain scratch/fence and return a sanitized recovery error carrying the operation ID, never automatic reversal. Before-receipt staging failure is explicitly unrecorded scratch requiring manual inspection, not a replayable journal operation.

`recover <operation-id> --owner-stopped` is explicit user confirmation. Under the same repository lock it aborts an exact pre-exchange operation or repeats durability synchronization and commits an exact post-exchange operation. It never exchanges again. Committed/aborted cleanup retries use the immutable expected backup manifest to validate partial deletion; current-head/target mismatches stop without mutation. Rollback builds a new exact inverse proposal from durable historical trees and applies through the same publisher. Artifact snapshots are read back before their bindings enter Project; complete rollback bytes remain in the durable journal. Resolve only credentials already referenced by the selected Project/source Run for fresh secret scanning; missing references permit offline inspection, no model call or ambient discovery occurs.

Acceptance: user apply performs one atomic version update with exact audit/head/Project bindings. Concurrency, every publication cut point and stale Run/init/code-patch races fail safely. Explicit rollback creates/applies a new inverse and restores exact before bytes without erasing history. Unreferenced safe files survive unchanged.

### Milestone 3: complete Phase 6 behavioral and independent acceptance

Acceptance: public bootstrap/chat/proposal/diff/apply/new backend task/exact approval/independent Docker evidence/rollback journey proves the new command becomes mandatory only after application. Protected policy and verifier mutation denial remain unchanged. All quality, unit, contract, integration, offline E2E, full default, Docker and package gates pass on a frozen source identity; independent verifier returns an evidence-backed verdict before checkpoint.

## Detailed implementation steps

1. Finish Phase 5 gates/docs/checkpoint. Finalize the precise domain/port/concurrency contracts here and assign disjoint single writers before any Phase 6 source change.
2. Add strict workflow/skill models, reference closure and requirement consumption in config loading/TaskSpec construction; retain generated legacy valid files and exact old snapshot identity where no skill exists.
3. Harden the existing FleetPatch validator and establish bounded complete-tree/proposal/version/operation models plus generated schemas.
4. Implement exact filesystem read/stage/exchange/inspect/cleanup behind a project-owned port; verify native primitive semantics against primary platform documentation and test real supported filesystem behavior.
5. Implement migration 8 journal/head/claims and transaction-local project fence checks shared with ordinary/chat Run creation, continuation, registration and target patch application.
6. Add application proposal/diff/apply/rollback/recovery with exact artifact read-back and durable before/after audit. Wire the CoS output branch and thin CLI; no model-authorized apply surface.
7. Add normal and adversarial lifecycle/race/publication tests, real-Docker behavior and CLI E2E. Update specs/ADRs/README/acceptance ledger, run frozen full gates and commit/push only accepted behavior.

## Validation plan

Run `uv lock --check`, `uv sync --all-extras`, Ruff format/lint, `mypy src tests`, schema `--check`, unit/contract/marked integration/E2E/default suites, offline archives and `git diff --check`. Run all real Docker tests separately with explicit local Python/pytest runner; no live provider calls or ambient key use. Re-read managed containers and every acceptance state's outstanding leases/fences after tests.

Required negative cases: changed proposal/project/base/content/artifact hashes; protected-field/path aliases and ancestry; traversal/symlink/hardlink/special files; bounded tree/context/UTF-8/registered secrets; invalid schema/reference/capability; omitted baseline/conditional command; model apply attempt; changed exact command grant; two-process apply race; stale normal/chat registration and init/code-patch apply; failure before/after prepare, exchange, fsync and SQLite commit; ambiguous restart; current-only rollback and unreferenced-file preservation. Assertions use real persisted state/events/bytes/evidence, not callback counts.

## Rollback and recovery

Source checkpoints remain recoverable Git commits; never reset unrelated work. State migration is transactional/forward-only. Configuration rollback is a new reviewed inverse operation and cannot edit trust/audit or silently undo a newer version. Incomplete publication retains exact hashes/owner/fence and refuses new execution until explicit safe reconciliation. No automatic timeout takeover, duplicate exchange, broad scratch removal, target reset or guessed restoration is allowed.

## Progress

- [x] (2026-09-05) Read-only application/config and persistence/publication audits ran alongside frozen Phase 5 gates. Recorded this living plan before Phase 6 implementation.
- [x] (2026-09-05) Established that opaque workflow prose, bootstrap create-only publication and referenced-only ConfigSnapshot cannot independently meet operational evolution requirements.
- [x] (2026-09-05) Independent read-only design review identified five contract refinements now recorded above: monotonic head/tree admission, exact Git dirty-state rebinding, staged-file durability and recovery identities, explicit skill reference closure, and immutable prior bytes sufficient for rollback after backup cleanup. These are required design constraints, not implementation or acceptance claims.
- [x] Phase 5 accepted checkpoint; exact Phase 6 contracts and writer ownership frozen.
- [x] (2026-09-05) Phase 5 checkpoint `a46b68800fb9725274db869f8c360b97a70c24ef` committed from a clean accepted tree and pushed with exact remote read-back. The previous phase gate is satisfied; freeze each disjoint implementation slice contract below before assigning its writer.
- [x] Typed executable configuration and persisted CoS proposals are implemented with focused evidence below; final whole-phase gates remain separate.
- [x] (2026-09-05) Slice A focused config/adapter/evidence integration: 171 passed in 40.46s; default file/snapshot byte identity preserved. Root task/evidence integration plus existing workflow regression group: 42 passed in 164.26s. Fake receipts demonstrate enforcement and identity separation only, not behavioral execution. Full Phase 6 gates remain open.
- [x] (2026-09-05) Slice B FleetPatch/runtime-model regression group: 135 passed in 0.13s (128 FleetPatch cases); owned formatting/lint/type/diff clean. Bounded context initial unit group: 9 passed in 0.04s; subsequent lint found one list-style issue, corrected before further integration. Slice E legacy conversation integration: 6 passed in 24.73s; graph contracts excluding migration-count fixture: 33 passed in 1.28s. Journal/headed hook and native publication acceptance are not yet complete.
- [x] (2026-09-05) Slice E headed registration/admission tests: 29 passed in 1.40s, scoped Ruff/mypy/diff clean. Initial root schema7 upgrade regression failed after 9 passing context tests because the new helper rejected real pre8 databases. Journal writer fixed compatibility only for contiguous pre8 migrations with no partial organization tables; named upgrade plus E hooks then passed 30 tests in 1.38s. Schema8 missing/partial tables still fail closed.
- [x] Atomic version apply, project fencing and inverse rollback/recovery are implemented with focused joint native/SQLite/CLI evidence below; platform/full acceptance remains separate.
- [x] (2026-09-05) Slice C frozen: `uv run pytest -q tests/unit/test_organization_tree.py tests/contract/test_organization_publication.py` with Docker/live disabled: 115 passed in 1.49s; all five owned files passed Ruff format/lint and mypy. Real macOS exchange/reopen/metadata/locking/interrupted-cleanup evidence; Linux execution remains separate. Unsupported or unreproducible metadata fails closed, pre-receipt scratch is retained.
- [x] (2026-09-05) Slice D frozen: organization journal/admission, graph/conversation store and conversation-state regression group: 157 passed in 10.74s; scoped format/Ruff/mypy/diff clean. Journal fixtures prove persisted states and exact claims, not filesystem effects. Slice F frozen: pure proposal-hash catalog plus runtime-tool regressions: 47 passed in 0.07s (39 new); scoped static gates clean. Root now wires only this pure utility into CoS and documents its use in the prompt.
- [x] (2026-09-05 17:17 UTC) Full Phase 6 behavioral gates and independent verdict passed; metadata/documentation/checkpoint tracked below.
- [x] (2026-09-05) Root init/CLI/provider regression rerun after preserving Fake CoS's empty catalog: 55 passed in 52.59s. Full unit/contract diagnostic run (not final frozen acceptance): 1574 passed in 79.91s. Whole `uv run mypy src tests` passed on 208 files at that point; after further interface/presentation tests it passed on 210 files. Schemas now export 82 models, including typed skills and durable organization records; phase marker remains 5.
- [x] (2026-09-05) Joint filesystem/SQLite failure matrix initially passed 8 tests in 30.45s: before/after prepare and commit, before/after exchange, distinct cleanup gaps, and deliberately unrecorded lost staging receipt. Chat/proposal/generation/broad-scope plus existing conversation safety/reopen regression then passed 44 tests in 122.82s. Corrected two test-only role comparisons (`RoleId` is a validated string, not Enum identity) and added an explicit broad-scope assertion so the test cannot silently miss its branch.
- [x] (2026-09-05) Historical duplicate chat submissions now validate their original goal/summary/options and durable turn binding, then inspect the original Run without provider preflight or organization re-admission. A different goal/options fails; duplicate reads cannot reset budgets, settle ownership or replay work. Required for historical proposals after publication/rollback and when the original credential is unavailable. This changes no stored Run/turn wire fields or hashes; existing 29-case independent safety regression is included in the 44-pass group.
- [x] (2026-09-05) Expanded native-flush/partial-cleanup/active-publisher and CLI suite initially returned 27 passed, 3 failed in 66.40s. Two fixture extras changed exact untracked Git status before baseline; the test now explicitly seeds that reviewed fixture allowance before any head. The flush probe initially mistook the native capability test for the actual `.fleet` exchange; it now injects failure only after that exact target exchange. No production boundary was weakened. Expanded repeat passed 30 tests in 81.38s, including all twelve joint recovery cases, four interfaces, seven proposal/publication cases and schema/presentation checks.
- [x] (2026-09-05) Root offline FunctionModel proposal integration: 5 passed in 4.47s after correcting test harness expectations (explicit required start arguments; invalid output raises a typed error and leaves a persisted FAILED Run). Actual CoS hashing/output and exact no-apply artifacts passed. Added real-directory application and rollback: 7 passed in 17.33s, including tracked/untracked Git allowance, exact historical bytes/metadata and read-only committed repeats.
- [x] (2026-09-05) Root initialization/code-patch fencing and thin fleet-patch CLI are integrated. First project/CLI regression group: 53 passed, 2 failed in 42.07s; the 55-pass repeat above supersedes it. Root cause was the deterministic Fake CoS deliberately rejecting any nonempty tool catalog; keep its established empty catalog and expose pure hashing to the real runtime adapter. This preserves Fake fixture behavior without weakening any resource permission. Conversation artifact references now explicitly accept the two proposal kinds; old serialized references remain unchanged.
- [x] (2026-09-05) Public bootstrap/chat/proposal/apply/backend work/explicit code apply/organization rollback Docker journey: `AGENT_FLEET_ENABLE_DOCKER_TESTS=1 AGENT_FLEET_ENABLE_LIVE_PROVIDER_TESTS=0 AGENT_FLEET_DOCKER_TEST_IMAGE=agent-fleet-runner:phase5-chat uv run pytest -q tests/docker/test_fleet_evolution_journey.py` passed 1 test in 26.38s, using the accepted local runner image `sha256:15245c81b1efb7a68bad269f03737955eb5e6a2b7fc79aa98faa49c93a899851`. The model is an offline FunctionModel, not a provider call. Each distinct Engineer/Verifier executed both the five-test baseline and the two-test integration command; the bundle has four real command receipts and no proof gaps. Exact approvals survived container reconstruction, source changed only through explicit code apply, rollback restored the complete organization tree, grants were unchanged and no exact installation resources remained.
- [x] (2026-09-05) Added fresh-process offline chat/proposal/publication and abrupt process-exit recovery E2E: three passed in 28.99s. Initial setup returned three errors in 0.87s because the fixture selected a real adapter without credentials. Corrected only setup to register the offline FunctionModel before initialization; no credential was discovered/configured. Abrupt exits after durable preparation and native exchange leave a real recoverable fence, and new CLI processes require explicit owner-stopped confirmation before aborting/committing without another exchange.
- [x] (2026-09-05) Expanded joint recovery to 17 cases, all passed in 66.79s. Exact source/index-flag/organization drift is preserved; before-preparation rejection cleans only known scratch, while post-exchange drift remains fenced and rejects recovery until the test explicitly restores only its own deliberate edit. No product overwrite or automatic rollback occurs.
- [x] (2026-09-05) Initial frozen checksum `1c4f51631c8205e3741d8b7367d98a5ef85a8a42e896e98f85d822af52c5471e`: lock51/sync49, Ruff247 files/lint, mypy213, 82 schemas and whitespace passed. Unit1010/57.13s; contract567/29.81s; complete Docker14 passed4 deselected/141.89s. Default and integration found two legacy test assumptions and were deliberately interrupted: default2 failed746 passed14 skipped/730.56s; marked integration2 failed157 passed85 deselected/638.24s. Subsequent E2E22 passed/220.49s. These are retained diagnostic observations, not accepted whole-phase gates.
- [x] (2026-09-05) Corrected legacy credential tests: keep startup CONFIG_INVALID but expect the earlier native resumed-tree RECOVERY_REQUIRED; check no secret/head/state mutation. Test a credential-rotated pre8 history in a separate test-owned database copy, preserving the actual headed fixture and never weakening production Project guards. Focused fresh-boundary repeat3 passed7 deselected/7.80s. An initial correction matched the wrong start/resume assertion; its in-flight14-case run returned2 failed12 passed/57.40s and is superseded by the corrected repeat. Obsolete user remediation suggesting headed init/rebinding was corrected in workflow/sandbox diagnostics, without changing authorization.
- [x] (2026-09-05 16:12 UTC) Freeze `ee9662bbf3dba07f93bf745f3e976776a88fd123539d5d8dc65389f021d6d1f5`: static/schema checks passed; unit1010/57.68s, contract567/30.83s, Docker14 passed4 deselected/156.89s, CLI/config regression40/97.72s and distribution/schema6/3.23s. Independent read-only root-integration audit returned PASS with identical start/end hash and fresh36/226.85s (offline FunctionModel, native macOS, SQLite and CLI processes; not live provider/Linux/Docker). Docker read-back found26 distinct databases with zero outstanding leases/pending heads, nine historical RECOVERED leases and two published versions; zero globally managed containers.
- [x] (2026-09-05 16:12 UTC) Full default stopped at1 failed890 passed14 skipped/1297.84s; marked integration at1 failed212 passed85 deselected/860.81s. The remaining legacy foreign-TaskSpec fixture inserted a Run without its now-required organization admission/config identity; the journal correctly rejected preparation before the artifact-integrity assertion. Correct only this fixture to use the real admission guard; do not relax production invariants. Preserve these failed gates and rerun the complete selections without fail-fast so any further compatibility failures are all reported.
- [x] (2026-09-05 16:16 UTC) Strengthened F6-09: real admitted backend TaskSpecs at revision0 and after rollback revision2 require only python-test, pause under Safe mode before any command receipt, and cancel with exact unchanged target/zero leases. The revision1 task still freezes both commands and retains four independent real receipts. Expanded public Docker journey1 passed/32.41s; corrected TaskSpec group6 passed/31.37s. A tests-only mypy invocation lacked source/conftest roots and returned38 import-derived errors; the required `uv run mypy src tests` passed213 files. Ruff248, lint, schema82 and whitespace passed.
- [x] (2026-09-05) Final test-only freeze `6eba65d4b190a8d8cfa3a3c16cc11e126d4f807bd885db27956cbc266cd832c2`; production-source-only checksum `7a5ef57c2c92f652fd5edc76881eaa7244897a13fcbcca510ddd19462bcc2fa8`. Independent read-only delta review confirmed the prior integration PASS remains applicable, with both start/end hashes unchanged; it did not duplicate tests/Docker. Full default, each named layer and complete Docker repeats now run without source/test edits. Documentation/guide drafting remains outside the freeze and does not claim Phase 7 source implementation or installed-user acceptance.
- [x] (2026-09-05 17:17 UTC) Final complete behavioral gates passed at the repaired freeze below. Metadata/docs/archives and Phase 6 checkpoint follow; no final merge is claimed yet.
- [x] (2026-09-05 16:41 UTC) Final marked integration returned1 failed283 passed85 deselected/1196.66s; its chained E2E continues and default is still diagnostic-running. `test_apply_rechecks_exact_configuration_snapshot` correctly found a public error compatibility regression: the new organization guard rejects valid configuration drift earlier as CONFIG_INVALID, hiding the established PATCH_TARGET_DIVERGED code. Preserve the public patch contract by translating only organization-guard entry CONFIG_INVALID into PATCH_TARGET_DIVERGED while holding that guard across application. Do not translate recovery/credential/integrity errors or errors from the actual apply body. Strengthen stale-generation/rollback assertions and rerun focused/static/full gates on a new freeze. This is a compatibility repair, not a relaxation or a test-only expected-value change.
- [x] (2026-09-05 16:45 UTC) Applied the narrow patch error-compatibility repair; original drift test still expects PATCH_TARGET_DIVERGED and additionally proves Run unchanged. Rollback/README-only generation tests now require the same error with no cause/context. Ruff249, mypy213, schema82 and whitespace passed. Refreeze: source/tests `e1d946ceeeb2b5ec7f3a47d7c6353ce6b6a0b5f35b740f818040188d386c9ffb`; source-only `b35653c7d6dedbaaf51644bfae31274ecf6e9f1f863be9585a5ab5422be4e5ab`. A final all-layer default run now records JUnit evidence and durations; named focused compatibility, independent delta review and complete Docker repeats run separately. The full selection includes every unit, contract, marked/unmarked integration and offline E2E test; use its partition counts as partitions, not invented independent suite results. This avoids duplicating the same long default/integration failure without omitting any layer. The prior chained E2E completed22/288.16s; it remains prior-freeze evidence.
- [x] (2026-09-05) Prior full diagnostic run finished1 failed1972 passed15 skipped/1753.32s, confirming the same single error-code regression and no additional failures. Final compatibility/config/workflow/TaskSpec group57 passed/291.68s; complete Docker repeat14 passed4 deselected/178.84s. Independent delta review PASS with unchanged final source/test hashes and six fresh in-memory probes: translate only guard-entry CONFIG_INVALID, preserve all other entry/body/exit errors, do not execute rejected body, retain the lock through application and release afterward. No provider/Linux test was implied. Final default JUnit run remains open.

- [x] (2026-09-05 17:17 UTC) Final source/test freeze `e1d946ceeeb2b5ec7f3a47d7c6353ce6b6a0b5f35b740f818040188d386c9ffb` remained unchanged through `AGENT_FLEET_ENABLE_DOCKER_TESTS=0 AGENT_FLEET_ENABLE_LIVE_PROVIDER_TESTS=0 uv run pytest -q --durations=15 --junitxml=<temporary-report>`: **1973 passed,15 skipped in1499.51s**. JUnit proves zero errors/failures and all1988 cases collected. Passed partitions: unit1010, contract567, integration369, E2E22, Docker-directory offline4, live-readiness-only1. The skipped cases are14 gated real-Docker and1 gated live-provider; the readiness-only case makes no model call. These are one-run partitions, not separately executed suites.
- [x] (2026-09-05 17:17 UTC) On that same freeze, focused workflow/config/TaskSpec/interface regression:57 passed291.68s; complete explicitly enabled Docker:14 passed4 deselected178.84s. Image remains `sha256:15245c81b1efb7a68bad269f03737955eb5e6a2b7fc79aa98faa49c93a899851`. Fresh resource inspection found26 databases,97 released9 recovered leases, zero outstanding leases/pending heads, two published versions and zero managed containers. Independent delta PASS included six fresh in-memory probes proving only guard-entry CONFIG_INVALID is translated; other entry/body/exit errors and lock lifetime remain intact. No live/Linux execution was inferred.
- [x] (2026-09-05) Metadata5→6 changed only CLI marker and its two assertions. Final source/test checksum `6450c9c4d85bf1f9cf091de152389abcf0740dd0f2140f882c574c00478cb244`. `uv run pytest -q tests/integration/test_cli.py tests/integration/test_distribution.py tests/unit/test_schema_generation.py`:36 passed30.70s; Ruff249/lint,mypy213,82 schemas and whitespace pass. An initial schema refresh command incorrectly referenced a nonexistent scripts/export_schemas.py and stopped before tests; corrected to the documented `uv run python -m agent_fleet.schemas.generate --check`, then ran the entire selection successfully. No implementation was changed for that command typo.
- [x] (2026-09-05 17:24 UTC) Final README/archive/schema refresh:6 passed2.31s; whitespace and tracked product/doc machine-path/credential-shape inspection passed (only generic root/home prose matched the broad path query). Archives match207 package source files,82 schemas,eight migrations and five prompts. This remains an existing-dependency import smoke, not Phase7 fresh install.
- [ ] Checkpoint and exact remote read-back; self-referential commit/tree recorded in the next plan/delivery entry.

## Discoveries

- Observation: referenced workflow YAML is currently opaque and skills are absent. Consequence: add a narrow typed requirement path that actually affects immutable TaskSpec and final evidence; do not present prose changes as enforcement.
- Observation: logical ConfigSnapshot omits README/unreferenced content. Consequence: separately bind a complete tree manifest and preserve safe extras; a README-only version may change the tree hash without changing the logical configuration hash.
- Observation: bootstrap publication rejects replacement, and a symlink root is rejected by the loader. Consequence: use a dedicated same-filesystem real-directory exchange provider with no weaker fallback.
- Observation: config preflight and Run/Project writes are separated in time. Consequence: transaction-local project fencing plus expected configuration identity is required; a preflight list alone cannot serialize evolution against new execution.

## Decision Log

- Decision: protect FleetSpec, trust, credentials and sandbox ceilings; evolve existing role guidance and typed workflow/verification requirements. Rationale: the smallest working organization change does not need new authority or arbitrary roles. Date: 2026-09-05.
- Decision: stage privately beside, never inside, the repository; require same-filesystem atomic directory exchange. Rationale: retain a real `.fleet` directory and one visible full version without exposing temporary unreviewed files as repository content. Unsupported environments fail closed. Date: 2026-09-05.
- Decision: current-head rollback generates and applies a new inverse under the explicit rollback command. Rationale: the command is already exact user authorization; an extra mandatory apply step would surprise the ordinary rollback contract without adding authority protection. Initial design considered proposal-only rollback; refined before implementation after the CLI/spec audit. An optional preview remains possible, and all history/new IDs are retained. Date: 2026-09-05.

## Outcomes

Phase 6's full local behavioral acceptance passed at `e1d946ceeeb2b5ec7f3a47d7c6353ce6b6a0b5f35b740f818040188d386c9ffb`:1973 default passes with15 explicit skips, fourteen real-Docker passes and independent integration/delta PASS. CoS proposals, independently required verification skills, full-tree publication, current-head inverse rollback and exact recovery are connected; protected authority remains unchanged. Full gates exposed three legacy fixture assumptions and one public code-patch error compatibility regression; the retained failed results and narrow repairs above precede the successful final freeze. Metadata/archive refresh and Phase 6 checkpoint follow; Phase 7, Linux/fresh-install/live-provider acceptance and final GitHub merge remain separate.
