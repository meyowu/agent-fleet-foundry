[project]
name = "fleet-learning-canary"
version = "0.0.0"
requires-python = ">=3.12,<3.15"

[project.optional-dependencies]
dev = ["pytest==9.1.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
