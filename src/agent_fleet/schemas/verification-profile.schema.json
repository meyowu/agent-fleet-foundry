{
  "$defs": {
    "VerificationCommand": {
      "additionalProperties": false,
      "properties": {
        "argv": {
          "items": {
            "type": "string"
          },
          "title": "Argv",
          "type": "array"
        },
        "cwd": {
          "title": "Cwd",
          "type": "string"
        },
        "executable": {
          "minLength": 1,
          "pattern": "^(?:[A-Za-z0-9._+-]+|\\./[A-Za-z0-9._+-]+)$",
          "title": "Executable",
          "type": "string"
        },
        "networkRequired": {
          "const": false,
          "title": "Networkrequired",
          "type": "boolean"
        },
        "timeoutSeconds": {
          "maximum": 3600,
          "minimum": 1,
          "title": "Timeoutseconds",
          "type": "integer"
        }
      },
      "required": [
        "executable",
        "argv",
        "cwd",
        "timeoutSeconds",
        "networkRequired"
      ],
      "title": "VerificationCommand",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "apiVersion": {
      "const": "agentfleet.dev/v1alpha1",
      "title": "Apiversion",
      "type": "string"
    },
    "commands": {
      "maxProperties": 32,
      "patternProperties": {
        "^[a-z][a-z0-9_.-]*$": {
          "$ref": "#/$defs/VerificationCommand"
        }
      },
      "propertyNames": {
        "maxLength": 100,
        "minLength": 1
      },
      "title": "Commands",
      "type": "object"
    },
    "kind": {
      "const": "VerificationProfile",
      "title": "Kind",
      "type": "string"
    },
    "requiredForCodeChange": {
      "items": {
        "maxLength": 100,
        "minLength": 1,
        "pattern": "^[a-z][a-z0-9_.-]*$",
        "type": "string"
      },
      "maxItems": 32,
      "title": "Requiredforcodechange",
      "type": "array"
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "commands",
    "requiredForCodeChange"
  ],
  "title": "VerificationProfile",
  "type": "object"
}
