{
  "$defs": {
    "AcceptanceCriterion": {
      "additionalProperties": false,
      "properties": {
        "criterion_id": {
          "description": "Stable identifier for one requested task outcome.",
          "maxLength": 100,
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_-]*$",
          "title": "Criterion Id",
          "type": "string"
        },
        "description": {
          "description": "Observable task outcome, not a delivery-evidence category copied from required_evidence. For code changes, describe requested behavior that admitted independent checks can support; preserve requested scope and report unsupported proof rather than invent it.",
          "maxLength": 4096,
          "minLength": 1,
          "title": "Description",
          "type": "string"
        }
      },
      "required": [
        "criterion_id",
        "description"
      ],
      "title": "AcceptanceCriterion",
      "type": "object"
    },
    "WriterAssignment": {
      "additionalProperties": false,
      "description": "A bounded untrusted subgoal; only the planner may instantiate its writer.",
      "properties": {
        "criterion_ids": {
          "items": {
            "maxLength": 100,
            "minLength": 1,
            "pattern": "^[a-z][a-z0-9_-]*$",
            "type": "string"
          },
          "maxItems": 128,
          "minItems": 1,
          "title": "Criterion Ids",
          "type": "array"
        },
        "goal": {
          "maxLength": 8192,
          "minLength": 1,
          "title": "Goal",
          "type": "string"
        },
        "node_id": {
          "maxLength": 64,
          "minLength": 1,
          "pattern": "^[a-z][a-z0-9_-]*$",
          "title": "Node Id",
          "type": "string"
        },
        "role_id": {
          "anyOf": [
            {
              "maxLength": 64,
              "minLength": 1,
              "pattern": "^[a-z][a-z0-9_-]*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Role Id"
        },
        "scope": {
          "items": {
            "maxLength": 4096,
            "minLength": 1,
            "pattern": "^[^/\\\\\\x00][^\\\\\\x00]*$",
            "type": "string"
          },
          "maxItems": 128,
          "minItems": 1,
          "title": "Scope",
          "type": "array"
        }
      },
      "required": [
        "node_id",
        "goal",
        "scope",
        "criterion_ids"
      ],
      "title": "WriterAssignment",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Untrusted CoS proposal accepted only after control-plane validation.",
  "properties": {
    "acceptance_criteria": {
      "description": "Observable outcomes of the requested task. Do not blindly copy delivery requirements such as canonical_patch into behavioral criteria. Code-change proof requires admitted independent command evidence; unsupported outcomes remain explicit proof gaps.",
      "items": {
        "$ref": "#/$defs/AcceptanceCriterion"
      },
      "maxItems": 128,
      "minItems": 1,
      "title": "Acceptance Criteria",
      "type": "array"
    },
    "allowed_paths": {
      "items": {
        "maxLength": 4096,
        "minLength": 1,
        "pattern": "^[^/\\\\\\x00][^\\\\\\x00]*$",
        "type": "string"
      },
      "maxItems": 128,
      "title": "Allowed Paths",
      "type": "array"
    },
    "change_kind": {
      "default": "code_change",
      "enum": [
        "read_only",
        "code_change"
      ],
      "title": "Change Kind",
      "type": "string"
    },
    "fleet_strategy": {
      "description": "Smallest sufficient team strategy, independent of the declared workflow identifier. The control plane constructs its fixed role nodes; only parallel_engineers accepts writer_assignments.",
      "enum": [
        "direct",
        "single_engineer",
        "engineer_verifier",
        "parallel_engineers",
        "research_architect_engineer_verifier"
      ],
      "title": "Fleet Strategy",
      "type": "string"
    },
    "forbidden_paths": {
      "items": {
        "maxLength": 4096,
        "minLength": 1,
        "pattern": "^[^/\\\\\\x00][^\\\\\\x00]*$",
        "type": "string"
      },
      "maxItems": 128,
      "title": "Forbidden Paths",
      "type": "array"
    },
    "max_parallel_agents": {
      "default": 2,
      "maximum": 8,
      "minimum": 1,
      "title": "Max Parallel Agents",
      "type": "integer"
    },
    "normalized_goal": {
      "maxLength": 8192,
      "minLength": 1,
      "title": "Normalized Goal",
      "type": "string"
    },
    "required_evidence": {
      "description": "Required delivery/proof types, distinct from acceptance_criteria task outcomes: for example canonical_patch, command_evidence, independent_verifier_verdict. Direct read-only tasks retain control_plane_plan.",
      "items": {
        "enum": [
          "canonical_patch",
          "command_evidence",
          "control_plane_plan",
          "independent_verifier_verdict"
        ],
        "type": "string"
      },
      "maxItems": 32,
      "minItems": 1,
      "title": "Required Evidence",
      "type": "array"
    },
    "response": {
      "anyOf": [
        {
          "maxLength": 8192,
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Response"
    },
    "role_selections": {
      "additionalProperties": {
        "maxLength": 64,
        "minLength": 1,
        "pattern": "^[a-z][a-z0-9_-]*$",
        "type": "string"
      },
      "description": "Declared role overrides as a dictionary; use {} when unused, not null.",
      "maxProperties": 4,
      "propertyNames": {
        "enum": [
          "engineer",
          "verifier",
          "researcher",
          "architect"
        ]
      },
      "title": "Role Selections",
      "type": "object"
    },
    "workflow": {
      "default": "code-change",
      "description": "Exact declared workflow identifier from available_workflows, such as code-change. This is not a fleet strategy: engineer_verifier selects a team, not a workflow.",
      "maxLength": 64,
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9_-]*$",
      "title": "Workflow",
      "type": "string"
    },
    "writer_assignments": {
      "description": "Parallel Engineer shards only. Must be [] for direct, single_engineer, engineer_verifier, and research_architect_engineer_verifier. Do not list fixed Engineer or Verifier nodes here: the control plane constructs them. A Verifier is independent and never a writer. For parallel_engineers provide 2-8 bounded writers with unique node IDs, disjoint scopes, and exact criterion coverage.",
      "items": {
        "$ref": "#/$defs/WriterAssignment"
      },
      "maxItems": 8,
      "title": "Writer Assignments",
      "type": "array"
    }
  },
  "required": [
    "normalized_goal",
    "fleet_strategy",
    "allowed_paths",
    "forbidden_paths",
    "acceptance_criteria",
    "required_evidence"
  ],
  "title": "ScopeDecision",
  "type": "object"
}
